# Implementation Status — Fashion-Nova rebuild (living doc)

Single source of truth for what's **done**, **in progress**, and **on hold**.
Update this with every change (see CLAUDE.md "Keep docs in sync"). Older platform
phases (0–5) live in `ROADMAP.md`; deferred production choices in
`PRODUCTION_DECISIONS.md`.

Legend: ✅ done & deployed · 🟡 in progress · ⬜ planned · ⏸️ on hold (needs a decision)

---

## Task board (the 15 tasks) — durable record

> Mirror of the working task list so it persists in the repo. Keep statuses current.

| # | Task | Status |
|---|---|---|
| 1 | Catalog + IA data foundation (6 divisions, categories, collections, attributes) | ✅ |
| 2 | Fix left-sidebar category bug (per-division) | ✅ |
| 3 | Top nav + full-width mega menu (broad vs single-type) | ✅ |
| 4 | Listing pages + filters (`/collections?division=`, derived facets, toolbar) | ✅ |
| 5 | Landing page rebuild (hero, trend, bento, shop-the-latest, footer) | ✅ |
| 6 | Enrich PDP (details, materials, reviews, related/trending) | ✅ |
| 7 | Admin UX overhaul — Site Settings + CMS-managed landing + shadcn fields | ✅ Site Settings `e787ca9`; CMS landing + dynamic add/reorder `79dd28b`; shadcn Combobox + DatePicker `5d8d002` |
| 8 | Device image/video upload field (storage pluggable) | ✅ |
| 9 | Storefront bugs — product share + instant reviews | ✅ |
| 10 | Admin product form — division/occasion/style/trend/material/care + richer editor | ✅ |
| 11 | Discounts — start/expiry dates | ✅ |
| 12 | Checkout — shipping methods + payment options (verify + surface) | ✅ `d8c392d` — BD shipping ৳60 + COD verified; shipping editable in admin, payment card added |
| 13 | Auth — phone-OTP popup, registration (phone+OTP), email/phone login, intl phone, boxed OTP, auto-login | ✅ |
| 14 | Checkout — required fields + intl phone + prefill + persona section + stacked discount | ✅ `d5daa73` |
| 15 | Admin — persona builder (title/bracket/questions/promo code) | ✅ `b876f5b` |
| 16 | Required-field red star (*) across storefront + admin forms | ✅ `de2c591` product/discount/auth/reviews/phone/checkout (admin `required` prop available for any field) |
| 17 | Fix Vercel build fail — time-box sitemap product fetch (cold Medusa hung past 60s) | ✅ `32fc21b` |
| 18 | Multi-user admin auth + roles (replace single-password `ADMIN_PASSWORD` gate) | ✅ built (typecheck + build green; pending commit/deploy) — CMS `AdminUser` + `AdminRole` (ADMIN/EDITOR), scrypt-hashed passwords, HMAC-signed session cookie (`lib/session.ts`, edge-safe), `/admin/users` Team & Roles manager, role-gated middleware; first-run bootstraps an ADMIN from `ADMIN_BOOTSTRAP_EMAIL` + `ADMIN_PASSWORD` then retires it |

**Done: 18/18.** Multi-user admin auth + roles (#18) is implemented; see `docs/SMOKE_TEST_18-32.md` for the visual verification checklist. Follow-up: production needs a cms `db push` (adds `AdminUser`/`AdminRole`) — same as the `BRAND_CAROUSEL` enum note below.

> Full day-by-day history of the whole project: [BUILD_LOG.md](./BUILD_LOG.md).

---

## Round 2 — Fully dynamic, Shopify-style CMS (2026-06-09 feedback) ⬜ planned

> **Vision:** every part of the storefront — navigation/IA, mega-menu, landing,
> listing config, PDP content, shipping/payment, settings — must be **admin-managed
> and dynamic** (no hardcoding), like Shopify/other ecommerce CMS. The IA follows
> Fashion Nova: **Division (page `/pages/{division}`) → Collection (mega-menu item
> w/ multi-column popover) → Category (listing filter / special section)**.

| # | Task | Type | Status |
|---|------|------|--------|
| 19 | Restore curated landing (homepage no longer prefers stale seeded CMS home) | fix | ✅ `3e50050` |
| 20 | CMS-manage full landing — editable blocks | add | ✅ `c70705e` hero/promo/feature/sale + trend-card, brand-tile & **collab carousel slides** all editable (`landing.collabSlides` + `CollabSlideArray` editor); `/` renders a PUBLISHED `home` PageLayout via `SectionRenderer` when it has sections, else curated `<Landing>` (#19-safe); optional `BRAND_CAROUSEL` SectionType (schema+renderer) — **needs cms `db push` to add the enum to the DB before a BRAND_CAROUSEL section is created**. Prior: `a2dfd74`,`4da9987` |
| 21 | Section manager: add Create + Delete sections (dynamic) | add | ✅ `(phase1)` create/delete/reorder/edit live |
| 22 | Divisions as admin-managed pages `/pages/{division}` (underline active; per-division mega) | add | ✅ `fd226e8`,`71e9fa1` routing + underline + admin labels/badges (per-division landing content = future) |
| 23 | Admin collection → multi-column popover builder (data-driven mega menu) | add | ✅ `71e9fa1` /admin/navigation builds divisions→collections→columns→links; navbar renders it (fallback to auto) |
| 24 | Listing: generalize category checkboxes + special sub-type section (admin-config) | update | ✅ `listingConfig` SiteSetting + `/admin/listings`: per-listing Category facet show/hide, filter-group reorder, and curated tile row (any source/limit) — replaces the hardcoded Tops row |
| 25 | Migrate remaining admin SelectField → Combobox | update | ✅ `c9bb0d7` all section editors + price-list (incl. searchable category/collection) + campaign + product-creator now use Combobox; `EnumCombobox` helper narrows to the option union (no casts) |
| 26 | Dynamic payment methods management in admin | add | ✅ `a3c7437` `/admin/payments` — CMS `checkout` SiteSetting `paymentMethods[]` (id/label/description/enabled), Zod-validated, checkout renders enabled methods; `/complete` validates chosen method. Live Medusa providers shown read-only (BDT region). |
| 27 | Dynamic shipping methods/zones management in admin | add | ✅ `a3c7437` `/admin/shipping` — live Medusa option amounts (edit via Admin API) + per-option zone display + CMS `checkout.shippingMethods[]` note/visibility override; checkout shipping list filters hidden + shows notes. |
| 28 | Fix hero slide editor design/alignment issue | fix | ✅ `3e50050` |
| 29 | Product form: richer material & care (rich text) | update | ✅ `3e50050` |
| 30 | Move persona into checkout contact-info step | update | ✅ `3e50050` |
| 31 | PDP accordions: max-height + scroll | fix | ✅ `3e50050` |
| 32 | Admin-editable Shipping & Returns content | add | ✅ `3e50050` |
| 33 | Automated test setup — Vitest (unit/component) + Playwright (e2e) | infra | ✅ `b304366` — root `vitest.config.ts` (jsdom + RTL, React 19, `@/`→apps/web) + `vitest.setup.ts`; sample tests for `cn`, `Button`, `parseSiteSettings` (9 passing); Playwright `apps/web/playwright.config.ts` + `e2e/home.spec.ts` smoke (auto-starts `next dev` :3200, passes on placeholder data; 1 passing). Scripts: `test`, `test:watch`, `test:e2e`, `test:e2e:install`. Test files excluded from `tsc`/`build` (typecheck green). |
| 34 | `createsuperuser`-style admin CLI (`admin:create`) | infra | ✅ `9926671` — `packages/cms/scripts/create-admin.ts` inserts an admin straight into the CMS `AdminUser` table (reuses `createAdminUser` scrypt hashing); interactive (hidden password) **and** flag-driven (`--email/--name/--role/--password`), plus read-only `--list`. Auto-loads `CMS_DATABASE_URL` from `apps/web/.env`. Removes the need for the `ADMIN_BOOTSTRAP_EMAIL`/`ADMIN_PASSWORD` first-run dance. Run: `bun run admin:create`. Fixed via `74a07c0` (run through `bun` directly so interactive TTY works). |
| 35 | Admin sidebar: pin to viewport (was stretching with tall content → bottom whitespace) | fix | ✅ `b67d973` — `components/admin/sidebar.tsx`: `<aside>` now `sticky top-0 h-screen self-start`; nav `overflow-y-auto`. |
| 36 | CMS-driven home: build the published `home` PageLayout into a full Fashion-Nova composition (owner chose CMS-driven over curated `<Landing>`) | add | ✅ LOCAL — published `home` rebuilt to **10 sections** in Fashion-Nova order: `MARQUEE → HERO → VALUE_PROPS → PROMO_SPLIT → PRODUCT_ROW → TREND_RAIL → CATEGORY_GRID → COUNTDOWN → BRAND_CAROUSEL → BANNER` (idempotent `packages/cms/prisma/seed-home.ts`, `bun run --filter @ecom/cms db:seed-home`; only the `home` PageLayout is touched — nav/popups/site-settings untouched). Uses the 4 new section types from #45. **Live seeded 2026-07-10** (same 10 sections, via `seed-home.ts` against live). |
| 37 | Seed complete, realistic data (Medusa catalog: products/variants/prices/categories/collections + CMS content: home sections/nav/popups) so the site looks production-real | add | ✅ local + **LIVE seeded 2026-07-10** (owner-authorized) — `seed-fashion-catalog` against the live Medusa DB: 26 products, 18 categories, 4 collections, 248 inventory levels, BDT region, color×size variants, multi-currency prices, offers. Live Store API verified (`count: 26`). Helper: `scripts/seed-live-catalog.sh` (`1897539`). |
| 38 | Live login 500 — live Neon CMS DB missing `AdminUser`/`AdminRole` (P2021) | fix | ✅ resolved 2026-07-10 — live Neon CMS schema in sync (`prisma db push` reports no drift; `AdminUser`/`AdminRole` + all `SectionType` enum values present), live admin exists (`admin@gmail.com`, ADMIN, active), live `/admin/login` returns 200. |
| 39 | Process rule: every backend/frontend change must be applied to BOTH local and live (schema pushes, seeds, deploys) | infra | 🟡 ongoing — captured per owner instruction 2026-06-14. |
| 40 | Unify landing model → **CMS sections everywhere** (owner decision): make `/pages/[division]` render CMS PageLayout sections (not the curated `<Landing>`), and enrich section types to reach Fashion-Nova richness (trend-card grid, brand tiles, collab carousel, richer hero) | add | 🟡 step 1 done — `/pages/[division]` renders a published PageLayout whose slug = division handle via `SectionRenderer`, falling back to curated `<Landing>` when none. **Step 2 done (#45):** richness enrichment — 4 new section types (PROMO_SPLIT, TREND_RAIL, VALUE_PROPS, COUNTDOWN) added end-to-end + seeded into `home`. **Next:** seed per-division layouts. |
| 45 | Enrich CMS section types to Fashion-Nova richness — **4 new SectionTypes end-to-end** | add | ✅ LOCAL — **PROMO_SPLIT** (2/3-up promo image cards w/ overlay + CTA), **TREND_RAIL** (horizontal snap rail of tall labeled cards), **VALUE_PROPS** (trust-badge strip; icon key→lucide), **COUNTDOWN** (live ticking urgency banner, client component, falls back to heading when elapsed). Each: Zod schema in `packages/cms/src/schemas/sections.ts` (+ `sectionConfigSchemas` + `defaultSectionConfig`), `SectionType` enum value, storefront component in `apps/web/components/site/` wired into `section-renderer.tsx`, admin editor in `apps/web/components/admin/editors/` wired into `section-manager.tsx` (creatable list is `SECTION_TYPES`-derived, so auto-listed). Local cms `db push` added the 4 enum values + `prisma generate`. typecheck (all packages) + `bun run build` green. Live cms `db push` done 2026-07-10 — the 4 enum values (and `AdminUser`/`BRAND_CAROUSEL`) are in the production DB. |
| 41 | Phone-capture popup → **dedicated `/admin` screen** + dynamic text (owner decision): make title/body/button/success/enabled editable (currently hardcoded in `phone-capture-popup.tsx`), wire the popup to read it | add | ✅ — new `phoneCapture` SiteSetting + Zod schema (`lib/phone-capture-settings.ts`), `/admin/phone-popup` editor (RHF+Zod) saving via `POST /api/admin/phone-capture`, sidebar "Phone Popup" item, popup reads managed copy & `enabled=false` hides it. `db push` not required (uses existing `SiteSetting` table). |
| 46 | Fix Vercel build fail — `/cart` (+ other per-user pages) static-prerender hung on cold Medusa via `getNavData`/`SiteNavbar` (>60s timeout, like #17) | fix | ✅ `export const dynamic = "force-dynamic"` on `/cart`, `/track`, `/wishlist`, `/checkout/success` so they render at request time (not prerendered at build). **Root fix:** `medusaFetch` now time-boxes every Store API call (`AbortSignal.timeout(10s)`) — a cold Render backend *hangs* the connection (never errors) so the existing try/catch never fired; on timeout it aborts → returns null → callers fall back to placeholders, ISR fills real data once warm. This protects all ISR/static pages (`/` fell over next). Local `bun run build` → 49/49 static pages, green. |
| 42 | Seed: relatable, per-category product images (was rotating a generic Unsplash pool → unrelated photos); re-seed local | fix | ✅ LOCAL — `seed-fashion-catalog.ts` now uses a category-keyed `IMAGE_SETS` map (`imageSetKey`/`pairFor`); re-seeded, verified per-type (jacket→outerwear, dress→dresses, heels→shoes, kids→kids, beauty→beauty). **Live re-seeded 2026-07-10** (part of the #37 live seed). |
| 43 | Fix `/admin/listings` duplicate-key React error (division handle collides with same-named category handle after full seed) | fix | ✅ `app/admin/(panel)/listings/page.tsx` dedupes `targets` by handle (division wins). typecheck green. |
| 47 | Process rules (owner, 2026-07-10): no co-author lines in commits; 2–3 files per commit; status doc updated with each task; run pipeline agents properly; read code before changing; read docs at session start; R&D per task; ask when confused; minimal replies; repo docs = memory (`docs/LESSONS.md` added as do-not-repeat log) | infra | ✅ codified in `CLAUDE.md` + `docs/LESSONS.md` |
| 48 | Process rules pt.2 (owner, 2026-07-10): senior-developer mindset — standard practice, stay current with the stack, DRY/KISS/YAGNI/SOLID in all code, and proactively suggest better alternatives when the owner's approach is suboptimal (honest push-back before building) | infra | ✅ codified in `CLAUDE.md` ("Senior-engineer mindset") |
| 49 | Footer audit → CMS content pages: 9 footer links 404'd (`/help`, `/help/size-guide`, `/help/shipping`, `/help/returns`, `/about`, `/careers`, `/contact`, `/sustainability`, `/gift-cards`) + no legal pages. Built admin-managed content pages: `contentPages` SiteSetting (Zod, list of slug/title/body/enabled), root catch-all `app/[...slug]` renders them (11 seeded defaults incl. new `/privacy-policy` + `/terms`, linked in footer), `/admin/content-pages` editor (RHF+Zod, add/remove/hide pages, rich text). No `db push` needed. | add | ✅ `7f35f44`,`783a1e3`,`26b5c20` — typecheck+build green; 11 routes smoke-tested 200 / unknown 404; code-reviewed (fixed: revalidatePath on save, empty-list vs never-saved defaults, server-side HTML sanitize, reserved/duplicate-slug validation, React.cache, a11y + delete confirm) |
| 50 | Real transactional email via **Brevo** free tier (300/day): OTP delivered to the register email when `BREVO_API_KEY`+`EMAIL_FROM` set (devCode fallback otherwise), order-confirmation email on checkout complete (best-effort, never blocks the order) | add | ✅ `9c27ab5`,`15942d6` — code-reviewed & hardened: OTP delivery bound to the account's stored email (anti-takeover), `devCode` needs `OTP_DEMO_CODES=true` in prod, IP+phone rate limits. Owner setup = smoke-tests/50 |
| 51 | **SSLCommerz sandbox** online payment: "Pay Online — bKash / Card" checkout method → hosted gateway redirect (`/api/checkout/pay`), validator-API-verified callback completes the order (amount+cart matched; cart cookie not trusted cross-site), failed/cancelled banners, `/complete` refuses gateway method | add | ✅ `ee56a71`,`9d2f282`,`05d4873` — code-reviewed: validator-only trust, amount+currency+cart binding, IPN for browser-loss, cart-id shape check, replay-safe. Owner setup = smoke-tests/51 |
| 52 | **Shop the Look** (FN outfit tagging): `/admin/shop-the-look` click-to-tag editor (hotspot % coords + linked product, max 8/look, one look/product, `shopTheLook` SiteSetting), PDP renders numbered pulsing dots (server component, reduced-motion safe) linking each piece | add | ✅ `745077c`,`4473d01`,`a512b97` — focus-visible dots, revalidates affected PDPs on save; no `db push` needed |
| 53 | Per-division landing PageLayouts: `seed-divisions.ts` (+`db:seed-divisions`) rebuilds 6 published layouts — women/men/**plus**(=Curve route alias)/sport/kids/beauty, 5-6 sections each, Zod-validated, home untouched | add | ✅ `4235c6e` — seeded local + **LIVE 2026-07-10** (owner-authorized); all 6 live division pages verified 200 |
| 54 | Polish trio: navbar **search autocomplete** (debounced TanStack Query → `/api/search` proxy, combobox a11y), **branded 404** (`app/not-found.tsx`), **keep-warm** GH Actions ping every 30 min to Render `/health` | add/fix | ✅ `888b935`,`0efb3ed` — reviewed: option-role a11y fix, `/api/search` rate-limited, keep-warm at 10 min (UptimeRobot recommended long-term); `170cbe6` honest search-error row |
| 55 | Follow-up: auth form converted to RHF + Zod (was the last raw-useState form; per-field validation messages) | update | ✅ `5011afe` (superseded by #57 split) |
| 56 | Register 500→401 bug: local Medusa was down → `registerCustomer` threw (bare 500) AND the OTP was consumed, so retry hit 401. Fixed: `verifyOtp` retry-safe (correct code stays valid until expiry), register route try/catch → friendly 502 | fix | ✅ `72e3011` — E2E verified incl. retry-after-failure |
| 57 | Auth redesign (owner feedback 2026-07-11): **separate** `/account/login` (minimal card) and `/account/register` (editorial split-panel) pages; register is **2-step** — details → **Next** (auto-sends code) → verify screen with 6-box input, masked destination, 30s resend cooldown, "edit details" back-link; **OTP now 6 digits everywhere** (generator, verify APIs, boxes, phone popup + its default copy) | update | ✅ `cf46e7e`,`6a549f8`,`f2d9f72`,`410d867`,`e957906` + `228b1d0`,`9766dcf` (**standalone layouts** — auth pages render without site navbar/footer: logo + "Back to store" only) — E2E: 6-digit devCode, register 201, both pages 200, zero `<footer>`/navbar markup |
| 44 | Landing-style toggle → resolve the "two landing systems" inconsistency with an explicit **per-page** choice (admin) between the curated Fashion-Nova `<Landing>` and the CMS section layout, for the home page + each division | add | ✅ — new `landingMode` SiteSetting + Zod schema (`lib/landing-mode.ts`: record page-key→`"curated"\|"sections"`, `landingModeFor` defaults to `"sections"`), `/admin/landing-style` editor (RHF+Zod, one segmented "CMS Sections" vs "Fashion-Nova Landing" toggle per page) saving via `POST /api/admin/landing-mode`, sidebar "Landing Style" item (`LayoutPanelTop`). `app/page.tsx` + `app/pages/[division]/page.tsx` now read `landingMode` and gate: `"sections"`→published PageLayout when it has sections else curated `<Landing>` (= prior behavior); `"curated"`→always `<Landing>`. No `db push` (uses existing `SiteSetting` table). typecheck + `bun run build` green. |

| 58 | Local checkout `/api/checkout/shipping` 502: Medusa `list-shipping-options-for-cart` crashed (`undefined.fulfillment_sets`) because the sales channel kept a **live link to a soft-deleted stock location** ("European Warehouse", deleted 2026-06-02, link not cascaded). Data fix: soft-deleted the stale `sales_channel_stock_location` row in the local DB; verified shipping returns ৳60/৳120 options. Live DB checked (read-only) — clean, no dangling links. No code change. | fix | ✅ data-only (local DB) 2026-07-11 |
| 59 | Professional order numbers: branded `MSN-00042` format via `lib/order-id.ts` (`formatOrderId`/`parseOrderId`) — used in all emails, checkout success, track page + lookup (API accepts "MSN-00042"/"#42"/"42"), admin orders list/detail/customer history | update | ✅ built 2026-07-11 |
| 60 | Emails/gateway links use the REAL request origin (`lib/origin.ts`: x-forwarded-host → host → url; localhost→http) — track links point at :3200 locally / live domain in prod; SSLCommerz callbacks no longer hit the wrong port (`.env` NEXT_PUBLIC_SITE_URL also fixed 3000→3200) | fix | ✅ built 2026-07-11 |
| 61 | Track page redesign: grid-column timeline (labels exactly under icons — old markup nested connectors inside steps), Ordered→Processing→Shipped→Delivered with dates, claret current-step ring, brass-rule result card, parcel links, empty/error states | update | ✅ built 2026-07-11 |
| 62 | Local Medusa backend admin login: `admin@maison.local` / `maison2026` at http://localhost:9000/app (created via `npx medusa user`) | infra | ✅ 2026-07-11 |
| 64 | Admin sidebar organized into labeled groups: Overview / Sales / Catalog / Content / Marketing / System (all items + roles preserved; Email Templates added under Content) | update | ✅ built 2026-07-11 |
| 65 | `/account` profile redesign: monogram header, personal-details card (now RHF+Zod), order history table/cards with branded IDs + status badges, address book grid — all via TanStack Query, same endpoints | update | ✅ built 2026-07-11 |
| 67 | Admin-editable **email templates** (`/admin/email-templates`): 5 branded templates (OTP, order confirmation, shipped, delivered, welcome) with subject/heading/body editing, placeholder legend, **send-test-to-inbox** (previews unsaved edits); fixed on-brand shell (ink band + claret rule + parchment card) in code; senders wired: otp/request, register (welcome), checkout complete + SSLCommerz callback (confirmation), ship (shipped + tracking no.), deliver (delivered). Stored in `emailTemplates` SiteSetting — no `db push`. | add | ✅ built 2026-07-11 — E2E: COD order MSN-00006 sent the templated confirmation |
| 68 | Footer content pages redesigned: hero band (eyebrow + oversized Fraunces title + brass hairline), styled body typography (standfirst first paragraph, hairlined h2s, brass list markers, claret links), closing contact CTA; richer default copy | update | ✅ built 2026-07-11 |
| 69 | **Real SMS OTP via MiMSMS** (owner purchased credit 2026-07-12): `lib/otp-sms.ts` implements the V2 transactional API (`POST api/V2/SMS`, type T, BD msisdn normalization, 15s timebox, throws surfaced errors). Env: `SMS_PROVIDER=mimsms` + `MIMSMS_API_KEY`/`MIMSMS_USERNAME`/`MIMSMS_SENDER_ID`. With SMS live, demo codes stay off and the phone-capture popup verifies for real. | add | 🟡 code ready; awaiting owner's panel username + sender ID + IP/domain whitelist to activate & live-test |
| 78 | MiMSMS **fully verified E2E** 2026-07-13: IP 74.220.48.20 approved, relay secret fixed, `campaignName` field added (their validator requires it despite "optional" docs) → real OTP delivered to the owner's phone via web→relay→MiMSMS ("SMS Send Successfuly", trxn GPK5AIF01AS0AFX, balance ৳700) | verify | ✅ SMS chain live |
| 79 | **Admin SMS templates** (`/admin/sms-templates`): company-name field + OTP & order-confirmation copy, live preview with sample values, GSM/Bengali segment counter, save rejects templates missing the brand name (operator rule). Defaults follow MiMSMS's documented OTP format + "(brand)" suffix. Senders read the setting: `sendOtp`, checkout complete, SSLCommerz callback. | add | ✅ `5c29ff7`,`6bba27f`,`7ce236e` |
| 80 | **Delivery partner switcher** (Shipping → "Delivery partner" card): Manual vs Steadfast + **test-mode toggle** (simulated consignments), stored in `courier` SiteSetting; handover route 409s when switched off; order-detail Ship step + button honor the choice; env `STEADFAST_MOCK` stays as fallback. | add | ✅ `274bb7b`,`91d9f5b`,`8db73e0` |
| 77 | **Steadfast TEST MODE** (owner: project is test-phase, Steadfast has no sandbox): `STEADFAST_MOCK=true` simulates consignments locally — `MOCK-…` id, `TEST…` tracking code, status auto-progresses Handed→In transit→Delivered over ~5 min, amber "TEST MODE — no real pickup" badge on the courier card. Real keys verified separately via `/get_balance` (status 200). Flip env off for real bookings. | add | ✅ `e1aaab0` — local env has mock ON; keys ready for real mode |
| 76 | **SMS relay on Render** — MiMSMS rejected dynamic-IP whitelisting (owner's ISP rotates IPs; Vercel has none fixed). New Medusa route `POST /sms` (secret-header auth, BD-number + size guards) relays to MiMSMS from Render's **static outbound IPs**; `lib/otp-sms.ts` dispatches via `MIMSMS_RELAY_URL`+`SMS_RELAY_SECRET` when set (direct mode kept for static hosts). Owner setup: Render env (MIMSMS_*, SMS_RELAY_SECRET) + whitelist Render outbound IPs + domain in MiMSMS panel. | fix | 🟡 built; awaiting Render env + MiMSMS whitelist |
| 70 | Phone popup OTP is 6-digit — confirmed already shipped with #57 (OtpInput default 6, popup validates 6) | verify | ✅ (no change needed) |
| 71 | Registration OTP **channel choice**: "Send my verification code by — SMS to my phone / Email" segmented picker; `/api/otp/request` honors `channel` ("sms" skips email — always safe, the SMS goes to the phone being verified); response reports channel + masked destination ("texted a 6-digit code to +8801•••••123") | add | 🟡 built (typecheck green; pending commit) |
| 73 | **Order confirmation SMS alongside email** (COD complete + SSLCommerz callback): `sendTransactionalSms` to the order phone — "Maison: order MSN-000XX confirmed (Tk …). Track: …" (phone captured pre-completion; best-effort, never blocks). Bulk foundation: `sendPromotionalSms` (MiMSMS type P, 1000/batch, dedupe) powering #72 | add | 🟡 built (typecheck green; pending commit) |
| 63 | `/admin/shipping` dynamic zones + options: zone cards (name + ISO-2 country chips, RHF+Zod edit/create), per-zone "Add shipping option" (name + flat ৳, mirrors seeded manual_manual/flat/BDT-region shape — payloads verified against the live local Medusa v2 API), delete option, delete empty zone. New helpers in `lib/medusa-admin.ts` (`listShippingZones`/`createShippingZone`/`updateShippingZone`/`deleteShippingZone`/`createShippingOption`/`deleteShippingOption`; zones read via `GET /admin/stock-locations` — v2 has no `GET /admin/fulfillment-sets`); routes under `/api/admin/shipping-options{,/[id],/zones,/zones/[zoneId]}` (Zod-validated, middleware-gated). CMS note/visibility overrides preserved. | add | ✅ built 2026-07-11 (typecheck + build green; pending commit) |
| 66 | `/admin/orders/[id]` order-status management surfaced: vertical status stepper (Ordered → Processing → Shipped → Delivered, timestamps + "Next" marker, canceled state) with ONE primary action for the next step — Start processing (fulfil) → Mark shipped (RHF+Zod tracking-number form) → Mark delivered — plus Cancel until shipped; each action toasts + refreshes. `packedAt` now mapped onto `AdminFulfillment`. | update | ✅ built 2026-07-11 (typecheck + build green; pending commit) |
| 75 | **Steadfast Courier integration (test-mode) + order handover**: `lib/steadfast.ts` env-driven client (`STEADFAST_API_KEY`/`STEADFAST_SECRET_KEY`, optional `STEADFAST_BASE_URL`, default `https://portal.packzy.com/api/v1`; headers `Api-Key`/`Secret-Key` — payloads verified against the official steadfast-it Laravel package source): `createConsignment` (POST `/create_order`), `getConsignmentStatus` (GET `/status_by_cid/:id`), Zod-validated, 15s timeboxes, status mapping pending→"Handed to courier" / in_review·hold→"In transit" / delivered\*→"Delivered" / cancelled\*→"Returned/Cancelled" (raw kept). `/admin/orders/[id]` Ship step now offers **Send to Steadfast** (only when configured; creates consignment with invoice=`MSN-XXXXX`, COD amount for COD orders, then normal `shipOrder` flow → shipped email + `steadfast.com.bd/t/<code>` tracking link on `/track`) alongside the manual tracking form; consignment stored in order metadata (`setOrderMetadata`); "Courier: Steadfast" card shows consignment id, tracking link + live status via TanStack Query (GET `/api/admin/orders/[id]/steadfast`, refresh button). No keys → option hidden, manual flow untouched. | add | 🟡 built 2026-07-12 (typecheck green; uncommitted; awaiting owner's Steadfast keys to live-test) |
| 72 | `/admin/customers` marketing tools: **promo SMS composer** (RHF+Zod; 160-chars/segment GSM vs 70 Bengali/Unicode with live segment count; audience = "all with a phone" or checkbox-selected rows; typed `SEND <count>` confirmation with ~৳0.35/SMS/segment cost estimate; POST `/api/admin/customers/sms` resolves phones server-side — "all" paginated from Medusa, "selected" by customer IDs only — then `sendPromotionalSms`; "SMS is not configured." surfaces as a banner) + **CSV export** (GET `/api/admin/customers/export` streams Name/Email/Phone/Joined/Orders for all customers, 500/page capped at 10k, quote-escaped + formula-injection-safe, `customers-YYYY-MM-DD.csv`) + row checkboxes w/ select-all-on-page (phone-less rows disabled); `AdminCustomerRow` now carries `phone`, table shows a Phone column | add | ✅ built 2026-07-12 (typecheck green; pending commit) |
| 74 | **Bulk product upload via Excel** (`/admin/products/import`, sidebar Catalog → Import Products): GET `/api/admin/products/import/template` generates an .xlsx (exceljs — owner-approved dep) with a "Products" sheet (styled/frozen header, 2 skipped EXAMPLE rows, 200 template rows, dropdown data-validations for division/category/collection/size-set/offer-type sourced from a hidden Lists sheet with LIVE category+collection handles, whole-number checks on prices/percent) + an "Instructions" sheet explaining every column; POST `/api/admin/products/import?mode=preview\|commit` (multipart `file`, caps 200 rows/5MB) parses with defensive cell coercion (`lib/product-import.ts`), validates every row against `productSchema` + live handle maps with per-field messages, preview returns green/red row table, commit creates valid rows sequentially via `createProduct` (now supports `collectionId`→`collection_id`) and reports per-row created/failed. One row = one product; comma-separated multi-value cells; swatches auto-derived from color names. | add | ✅ built 2026-07-12 (typecheck green; E2E verified locally: template → fill 2 rows → preview 2 valid → commit created `prod_01KX9GVR31ED5H39VXEKR2S26M` + `prod_01KX9GVRAX4JXKHT1Y3R51HT1T` with full metadata/variants/collection → deleted; catalog back to 26; uncommitted) |
| 81 | **Fix `/admin/customers/[id]` crash** — Medusa returns `null` (not `"not_fulfilled"`/`"not_paid"`) for computed order statuses when orders are fetched as a customer relation expansion; `o.fulfillmentStatus.replace()` threw a server TypeError. `getCustomerDetail` mapper now coalesces `fulfillment_status ?? "not_fulfilled"` / `payment_status ?? "not_paid"` (raw types corrected to `string \| null`) | fix | ✅ built 2026-07-16 (typecheck + build green) |
| 82 | **Phone-OTP (no-name) customers readable in admin** — phone signups have empty names + synthetic `p<digits>@phone.maison.local` account emails. `PHONE_EMAIL_DOMAIN` now exported from `lib/medusa-admin.ts` (single source; `customer-auth.ts` reuses it); list + detail mappers fall back name → phone → "—" and null out the synthetic email (list/detail render "—"/hide the block; CSV export writes empty) | fix | ✅ built 2026-07-16 (typecheck + build green) |
| 84 | **Mega nav parity with Fashion Nova (verified against FN's live nav data)** — nav schema: per-link `highlight` (accent red, e.g. "Sale Dresses") + `swatch` (color dot), per-collection `image` (promo tile); `MegaColumn` supports the FN headingless "Featured/lifecycle" first column; `AdminMegaPanel` renders up to 6 tracks + promo image tile; navigation builder edits all new fields | update | ✅ built 2026-07-16 (typecheck green; E2E visual verified on :3200) |
| 85 | **Mobile drawer renders the admin nav** — same source as desktop (admin collections → columns w/ headings, swatches, sale red, nested accordions), auto-categories fallback when a division has no admin nav | fix | ✅ built 2026-07-16 (visually verified at 390px) |
| 86 | **"Generate from catalog" navigation** — `POST /api/admin/navigation/generate` builds the FN column pattern (lifecycle / Shop By Style / Occasion / Trend / Color w/ swatches) per division from the LIVE catalog via `fetchListing` scoped facets (every link backed by ≥1 product by construction); per-category promo tile = a real product thumbnail; keeps division labels/badges, skips product-less divisions; one-click button (confirm-gated) in `/admin/navigation` | add | ✅ built 2026-07-16 |
| 87 | **Menu → listing → PDP flow verified** — generated nav for all 6 divisions (summary: women 8 / men 8 / plus 7 / sport 6 / kids 6 / beauty 5 items); ALL 254 unique generated links curl-checked on local: 0 empty listings; desktop mega panel + mobile drawer screenshots verified | verify | ✅ 2026-07-16 |
| 88 | **Fix: nav division context lost on listings** — `/collections/jeans?division=men` showed WOMEN active; navbar division detection now honors the `?division=` query param first (Suspense-wrapped `useSearchParams`) | fix | ✅ built 2026-07-16 (verified: MEN active + men's collection bar on listing pages) |
| 89 | **Fix: product edit wiped merchandising metadata** — edit page omitted `division/occasion/style/trend/material/care` from the form's initial values → fields showed blank on reopen and the next save erased them in Medusa (this is the "occasion/style/trends don't save" report; tags/type were already fine) | fix | ✅ built 2026-07-16 |
| 90 | **Division-aware landings (FN behavior)** — root causes: (a) `landingMode` SiteSetting was "curated" for every division, forcing one generic landing everywhere (flipped divisions→"sections" in local DB; LIVE needs the same flip in `/admin/landing-style` after deploy); (b) CMS product rails couldn't scope to a division — `productSourceSchema` now takes optional `division` on every kind, `fetchProducts` over-fetches + filters on metadata division, product-row editor gains a Division picker, seed rails now `newest+division`. Verified locally: men vs women landing product rails have 0 overlap | fix+add | ✅ built 2026-07-16 |
| 91 | **Gym products imported** — all 10 products from the owner's folder created locally with R2-hosted images (52+ images uploaded): women Soft Sculpt Flared Legging / Lift Seamless Midi Tank / Everyday Seamless Long Sleeve / Scrunch Leggings / Training Oversized Tee / Nova Gym Shorts (Brown+Navy+Maroon colorways, 12 variants), men Crest Long Sleeve / Arrival Woven Joggers / Crest Shorts / Arrival Tank. Division per owner rule (men folders→men, girls→women), category activewear+tops/bottoms, S–XL stock 25, images ONLY from each product's own folder (LESSONS #9). Verified: PDP renders, division listings show them, all images serve from R2. The 5 loose .mp4 videos not attached (products are image-based; usable later in CMS sections). **Live needs**: push + S3 env on Render + re-run the import against live | add | ✅ LIVE 2026-07-17 (10 products on live w/ R2 images; men/women activewear listings + PDPs verified) |
| 92 | **FN-parity product attributes & filters** — add length, sleeve, fabric, neckline, detail, print (+ price-range filter) to product schema/form/metadata, listing facets and filter rail; audit facet relevance per category | add | ⬜ planned |
| 93 | **Size guide** — per-product (or per-category default) size guide content replacing the single global modal | add | ⬜ planned |
| 94 | **Admin sidebar reorg (seller journey)** — group nav by task order: Setup (divisions/categories → shipping → payments) → Catalog → Storefront content → Marketing → Orders/Customers → Settings; make payments page state obvious (which methods are live in checkout) | update | ⬜ planned |
| 95 | **Admin simplification pass** — plain-language labels/help text on Navigation & Listings (generate-first flows), payments clarity, reduce jargon for non-technical sellers | update | ⬜ planned |
| 96 | **Catalog data hygiene** — root cause (audit): 3 stale seed scripts (`seed-bd-store`/`seed-rich-catalog`/`seed-mens-products`) redefined the same handles with wrong images/prices, and the docs pointed operators at them — running any silently overwrote the good catalog (the "child dress on men's denim" bug). Fix: deleted all 3, made `seed-fashion-catalog.ts` the single canonical seed, all doc refs repointed. Also added **verified menswear image pools** (Unsplash alt-text + 200-checked) so men's tops/bottoms/jeans get men's photos, and removed beauty products from the Accessories category. Verified: re-seeded local; men's products carry menswear. Rule recorded as LESSONS #9 | fix | ✅ built 2026-07-16 |
| 97 | **Cloudflare R2 storage + device upload EVERYWHERE** — owner supplied R2 keys (token since rotated; new secret in local env only); Medusa file module uses the S3 provider when `S3_*` env vars exist (local-disk fallback otherwise), `**.r2.dev` allowed in next/image. Verified E2E incl. after rotation. `MediaUploadField` (upload or paste URL) now on EVERY admin image input: navigation promo, storefront tiles/collab carousel, hero/promo/feature blocks, Shop-the-Look photo; product colors & mega-nav featured already had it. **Live needs**: the 6 `S3_*` env vars added in Render | add | ✅ LIVE 2026-07-17 (Render S3 env active; live upload → R2 verified) |
| 98 | **Modal + toast instead of browser alert/confirm** — new `ConfirmDialog` in `@ecom/ui` (centred modal, cancel focused so Enter never destroys, Escape/backdrop cancel, destructive + loading states); swept **all 13** admin components using `confirm()` (customer/product/order actions, discounts, price-lists, navigation, taxonomy, shipping, settings, users, sections, shop-the-look, content-pages). Zero browser `confirm()`/`alert()` left in admin. LESSONS #10 | update | ✅ built 2026-07-16 (typecheck + build green) |
| 92 | **Price-range filter** (FN-parity, judgment-based) — `pmin`/`pmax` params; `buildFacets` computes price bounds; `fetchListing` filters by range; universal "price" facet always shown (min/max inputs in the rail, swaps inverted ranges, counts as active, clears with Clear-all). Verified locally: ৳0–200 → 4, mid-range → 10, narrows correctly | add | ✅ built 2026-07-16 |
| 92b | **Apparel facets: sleeve, neckline, length, fabric, print** — five optional multi-value attributes end-to-end on the occasion/style/trend pattern: product form (comma-separated fields, edit prefills), Medusa metadata round-trip, listing engine (filters/facets/URL params `sleeve/neckline/length/fabric/print`), rail sections that auto-hide until a scoped product carries values, admin listing-config toggles. Facets appear per category as soon as products are tagged | add | ✅ built 2026-07-17 |
| 93 | **Per-product size guide** — optional `sizeGuide` (HTML/text) on the product schema, full Medusa-metadata round-trip (create + edit prefill), RichText field in the product form; PDP shows the product's own guide when set, else the global one | add | ✅ built 2026-07-16 |
| 94 | **Admin sidebar reorganized by seller journey** — day-to-day (orders/customers) → catalog → storefront → marketing → set-up (config last, Shopify model). The 9-item "Content" pile split into Storefront + Marketing; plain-language group descriptions; clearer labels | update | ✅ built 2026-07-16 |
| 95 | **Payments clarity** — page now leads with a live "what shoppers see at checkout" preview of enabled methods; each method card badges Live vs Hidden. (Broader admin plain-language pass is ongoing) | update | ✅ built 2026-07-16 |
| 99 | **Promotion usage limits + "coupon persists" explained** — investigation: storefront already isolates promos per cart (cookie cleared on completion; fresh cart has none); the reported carry-over is the **"Apply automatically" toggle** (automatic promos re-apply to every eligible cart, and nothing capped usage). Added campaign-budget limits to promotion creation: **max total uses** (`usage`) and **max uses per customer** (`use_by_attribute`+`customer_id`, 1 = one-time) + form picker + warning copy under the automatic toggle. Verified against local Medusa (budget created with limit 1) | fix+add | ✅ built 2026-07-16 |
| 100 | **Shop the Look + visual search accuracy — FIXED** — one server-side descriptor (sharp) for BOTH query and index: any image format (old code silently dropped every non-JPEG, incl. all R2/webp product photos), attention-crop centres on the garment, mean-subtraction removes the shared studio-background component; shopper queries now upload the image file (browser-computed vectors removed — they never matched the index); products auto-(de)index on create/update/publish/delete; reindex prunes against the catalog (transient failures can't shrink the index); admin page = one "Rebuild index" button. Verified locally: 36/36 indexed 0 failed; alternate-photo joggers query ranks the right product #1. **Upgraded to CLIP 2026-07-18**: `Xenova/clip-vit-base-patch32` (free open model, quantized ~90MB) self-hosted via `@huggingface/transformers` — true semantic matching (garment type/cut, not colours). Verified: the brown-shorts query the colour matcher ranked #4 now ranks the right product #1; joggers 0.93. Model lazy-loads per instance (cold first search ~10–30s, warm ~50ms); zero per-search cost, no external API. **Follow-up 2026-07-18 (owner report: irrelevant results)**: root cause was catalog data + UX, not the model — every seeded product in a category shared ONE thumbnail (equal vectors; all dresses identical), and the panel always showed the top 24 regardless of closeness. Fixed: per-product image offsets in the seed (0 duplicate thumbnails, local + live reseeded, gym re-imported, nav regenerated, reindexed), and a relevance cutoff (score ≥ max(0.6, top−0.07), cap 12) with an honest no-close-match empty state. Live verified: dress photo → exactly 1 result (the dress, 0.98); shorts photo → correct #1. Shop the Look: editor now uploads a real outfit photo (R2) — changing the photo clears stale tags | fix | ✅ built 2026-07-17 |
| 101 | **Live slowness diagnosed + fixed (code side)** — measured live: Render Medusa was fully asleep (30s–3min wake; the GitHub keep-warm cron actually fires ~every 2h due to GitHub throttling, not every 10 min), Neon suspends after 5 min, and the storefront's product cache expired every 15s with **dead tags** (nothing called revalidateTag). Fixes: commerce cache → 300s with `revalidateCommerce()` busting tags on every admin product/category/price mutation (admin changes still appear instantly); new `/api/warm` (touches Neon + Medusa); workflow improved as backup. **Owner action (5 min, free): two UptimeRobot monitors** — steps in GO_LIVE_GUIDE §11. Before serious launch: recreate Render+Neon in Singapore for BD latency | fix | ✅ LIVE 2026-07-17 (deployed; owner's UptimeRobot monitors active incl. /api/warm) |
| 83 | **Delete customer (ADMIN-only)** — `deleteCustomer` helper, `DELETE /api/admin/customers/[id]` (orders survive as guest orders), middleware `ADMIN_ONLY` consolidated to the whole `/api/admin/customers` subtree (sms + export + delete), `CustomerDeleteButton` (confirm → toast → refresh; `redirectTo` on detail) as trash row-action in the customers table + "Delete customer" on the detail header | add | ✅ built 2026-07-16 (typecheck + build green) |
| 102 | **Popup image editable in admin** — the `Popup` schema already had an optional `media` field and the storefront renderer read `config.media?.url` (falling back to a hardcoded Unsplash photo), but the admin popup editor never exposed it, so the image couldn't be changed. Added a `MediaUploadField` (upload or paste URL — same component used across the admin) wired into the form: `mediaUrl` in the `Form` type, prefilled from `config.media?.url`, saved as `media: { url }` (omitted when blank). Now the popup image is set/changed from `/admin/popups/[id]` | fix | ✅ built 2026-07-18 (typecheck green) |
| 103 | **FN-parity Search By Image popover** (owner spec w/ FN screenshots 2026-07-19): navbar camera icon → popover with drag-and-drop zone, "Upload photo" button, "OR paste an image link" input (SSRF-guarded server fetch), "You can try one of these styles below" — 4 random indexed catalog tiles via `/api/visual-search/samples` (click = demo search), collapse chevron; searching overlay warns the first search can take up to a minute (cold model) | add | ✅ `52a7709`,`d764498` — visually verified on :3201; PDP "Shop Similar" modal unchanged |
| 104 | **FN-parity search discovery dropdown**: focusing the empty search box opens a 620px panel — division tabs (all 6, data-driven), Hot Searches chips + 3 numbered thumbnail columns (Top Searches = top categories / Trending = trend facet / Occasion = occasion facet), ALL derived live per division via `lib/search-panel.ts` + `/api/search/panel` (5-min cache, shuffled) so every link lands on a non-empty listing; typing ≥2 chars switches to the existing product-suggestion combobox | add | ✅ `345bd38`,`c788c9f` — verified Women + Men tabs render division-correct photos/labels |
| 105 | **Visual search results page `/search`** — "Your Image Results" as a FULL listing page: `VisualSearchQuery` Prisma model persists the resized query JPEG + CLIP vector + parts under a `resourceId` (URL shareable/refreshable, 30-day prune), `POST /api/visual-search/query` (multipart file OR `{url}`), listing engine gained an `ids` allowlist filter (score order preserved) so the standard filter rail/facets/pagination all work over image matches, `params.extra` carries `resourceId`/`part` through every facet link; floating query panel (minimizable) with "Upload New Image" | add | ✅ `95b4420`,`f2d47b8`,`a83b7fe`,`f346d7c` — E2E verified: sample click → division=women URL → 1 honest match (Satin Slip Dress) |
| 107 | **Visual search UX round 2** (owner feedback 2026-07-19): (a) popover ^ chevron now returns to the text-search dropdown (FN) instead of closing; (b) division misdetection (dress→kids) fixed — prompt ensemble w/ summed scores, kids requires a clear margin, detected dress biases women; (c) seamless popover→results transition — searching overlay stays until /search arrives, results panel renders top-right in the popover's spot (was bottom-right); (d) FN-style translucent highlight box over the selected garment + "Showing: X" label; (e) dot taps now category-scope (bottom→bottoms/jeans, shoes→shoes…) with floorless crop ranking ordering within — explicit dots stay honest-empty rather than showing wrong-category items, auto-selected parts deselect when the catalog can't cover them. Most prominent garment auto-selected on landing (FN) | fix | ✅ `8ea65c7`,`b8149ea`,`e121755` — E2E: woman→women, man→men; top dot → 5 tops only; outerwear dot → jackets only |
| 111 | **FN structured size guides** (owner spec w/ FN screenshots 2026-07-19): `sizeGuides` SiteSetting — guides matched by category handles (+optional division, first match wins; defaults: Bottoms then Tops), FN modal via "View Size Guide": How-It-Fits customer bar, Fit Reference photo row (hidden until photos added), Measurements table with **in./cm toggle** (numeric ranges auto-convert ×2.54), numbered How-To-Measure; `/admin/size-guides` editor (guide list, fit-bar picker, fit photos via MediaUploadField, columns/rows, measure points) + sidebar entry; resolution: per-product rich-text override → structured guide → global text | add | ✅ `bf1fde9`,`a9d1edc`,`dd63340` — verified: gym shorts → Bottoms guide, cm toggle 24-25→61-64 |
| 112 | **PDP "We see similar styles" + FN Shop Similar** (owner's red-boxed module): card under the buy box with 3 overlapping thumbnails of the closest indexed styles + "Shop Similar" pill + "Style it with" (pieces tagged in this product's Shop-the-Look); Shop Similar now runs the current product photo through the visual-search engine → /search with garment dots (shoes auto-selected on the test product), size facets and similarity order — full FN-modal feature set via ONE engine; old basic ShopSimilarModal + visual-search context deleted | add | ✅ `26724eb`,`9930a91` — E2E: gym shorts PDP → Shop Similar → shoes highlighted, shoe-size facets, sandal result |
| 118 | **Owner feedback round (2026-07-20)**: (a) **per-division curated landing** — `site.landing` extracted to a reusable `landingSchema` + `landingByDivision` override + `landingContentFor` resolver; the Fashion-Nova landing now renders each department's own hero/promo/feature (mirrors CMS-sections per-page), edited via a **page selector** in /admin/site (inherits Home until customized; reset-to-Home per page). Verified: men override → "MENSWEAR EDIT" hero, home unchanged. (b) **size guide consolidated 3→2 places** — removed the redundant Brand&theme global text field; structured /admin/size-guides + per-product override remain. (c) **quick-shop modal always shows the color name** (+ swatches when multiple). (d) **`docs/SMOKE_TEST_FN.md`** added — owner checklist for every FN feature | fix | ✅ `8d2ce62`,`800fc6c`,`63e0128`,`e0bf2e5` — typecheck + build green |
| 119 | **Back-in-stock notify-me** (roadmap P1): sold-out sizes on the PDP show a bell + inline email capture (`RestockNotify`, `POST /api/restock`, rate-limited) instead of a dead "unavailable"; `RestockSubscription` model (idempotent per variant+email); `/admin/restock` waiting-list by variant with a one-click **Notify** that emails everyone via Brevo + marks them notified | add | ✅ `948d017`,`bc95377`,`e9bf4fd` — subscribe/dedup/notify-gating verified |
| 120 | **Admin analytics dashboard** (roadmap P1): the existing dashboard enriched with a **Restock-waiting** stat, **Top sellers** (units from order line items), **Recent orders**, and **Low-stock** (best-effort variant inventory); all graceful when data is absent | add | ✅ `4bcd813`,`8459ad5` |
| 121 | **Recently-viewed + abandoned-cart recovery** (roadmap P2): recently-viewed rail on the PDP (localStorage, dedup, excludes current — verified across 2 PDPs); `AbandonedCart` model, cart+email captured at checkout (debounced), marked recovered on order-complete; `/admin/abandoned-carts` list + one-click recovery email (Brevo) | add | ✅ `a24190f`,`dd3e38c`,`e37b4b3`,`6461a1e` |
| 122 | **Media library** (roadmap P2): every admin upload auto-recorded (`MediaAsset`), `MediaUploadField` gains **"Choose from library"**, `/admin/media` browse/copy-URL/remove | add | ✅ `7d4a21b`,`de09ffb`,`5aa8af6` |
| 123 | **Reviews-with-photos + Find-my-size + bundle pricing** (roadmap P3): review form photo upload (public rate-limited `/api/reviews/upload`) → thumbnails in the list + with-photos filter + sort; **Find my size** quiz matches body measurements against the structured size-guide table nudged by fit pref (verified: waist 29/hip 39 → M); **bundle pricing** — admin bundle % + promo code on Shop-the-Look, savings shown, code auto-applied when the whole look is added | add | ✅ `43fc541`,`4ddce75`,`370adc0`,`bb33432` |
| 124 | **Bulk product edit + scheduled content** (roadmap P3): multi-select in the products grid → bulk **publish/unpublish/delete** (`/api/admin/products/bulk`, confirm-gated); popup **start/end datetime** now settable in the editor (model + storefront date-filter already existed) | add | ✅ `011ac42`,`c7d2e55` |
| 125 | **Code-review hardening (#119–#124)** — P0s: review photo upload now RE-ENCODES every file through sharp (spoofed-MIME/arbitrary bytes rejected; EXIF stripped) and review-create is rate-limited with `photos` restricted to our own storage hosts (`lib/media-hosts.ts`) — kills the public arbitrary-upload/arbitrary-image vectors. P1s: bulk product publish/unpublish/**delete** is now ADMIN-only (middleware), user strings HTML-escaped in restock + abandoned-cart emails, media picker uses TanStack Query + Escape/focus, find-my-size Escape/aria-modal, checkout capture keyed on a cart signature (no re-POST on refetch), `Prisma.InputJsonValue` (no unsafe cast) | fix | ✅ `e2328e2`,`0d06e3b`,`0ac00b0`,`b195fb8` — verified: bad host 422, real image→R2, fake image 422 |
| 126 | **Auto-applied discount — root-caused & killed** (owner 2026-07-26: "a discount promo auto added in cart/checkout which I don't add"): cause = leftover **GET20** automatic promo (20% off order, `is_automatic: true`, created 2026-07-11 during #99 testing) in the **local** Medusa DB — automatic promos join every new cart by design. Set **inactive** via the admin API; verified fresh cart + add product → no promo, `discount_total: 0`. Live backend audited clean: only coded promos (PERSONA 3%, D 10 15%), none automatic; fresh live cart verified promo-free. Note: a promo already sitting on an old cart cookie (Shop-the-Look bundle code, checkout persona quiz, or a manually tried code) stays on that cart until removed (✕ chip in cart/checkout) or the order completes | fix | ✅ data change only (no code) |
| 127 | **/admin/discounts promo detail view** (owner: "can not see details of created promo in admin"): every row now expands (chevron on the code) into a detail panel — how it applies (automatic vs code-at-checkout), applies-to (entire order / shipping / resolved category & collection names), schedule (start → end or always-on), usage limit (X of N total used / N per customer / unlimited), created date, plus a warning callout on active automatic promos. `listPromotions()` now fetches `starts_at`/`ends_at`/`created_at`, `application_method.target_rules` (+values) and `campaign.budget` | add | ✅ `3115d2c` — typecheck green |
| 128 | **Editable promotions in /admin/discounts** (owner clarified #127: "not to see only details but edit the promo"): the expanded row is now a full **edit form** (RHF+Zod) — code/name, % or ৳ value, BOGO buy/get quantities, start/expiry dates, usage limit (total or per-customer, shows uses redeemed), automatic toggle; type & applies-to are immutable in Medusa (noted in the form — delete & recreate). Backend: `updatePromotion()` + extended `PATCH /api/admin/promotions/[id]`. **Regression found & fixed while building**: promo start/expiry dates were silently broken since the Medusa 2.15 upgrade — `starts_at`/`ends_at` (and usage budgets) live on the promotion's **campaign** now, top-level dates 400 ("Unrecognized fields"); `createPromotion` + `listPromotions` + `updatePromotion` all reworked to route schedule+cap through a per-promo campaign. Verified E2E on local Medusa 2.15.5: create w/ dates+cap, edit value/dates, cap 1-per-customer→total-100, cap removal, all reverted clean | add+fix | ✅ `1662e9e`,`<ui>` — typecheck + build green |
| 117 | **Code-review hardening (#113–#116)** — P1s fixed: shared `lib/safe-image-fetch` (SSRF-vetted, no-redirect, size-capped) now used by BOTH the public visual-search route and the admin auto-detect route (was a raw redirect-following fetch) + auto-detect rate-limited; "Add the whole look" no longer throws an unhandled rejection (adds what it can, reports the shortfall, prefers in-stock variants); uploading a new photo in Shop Similar now refreshes the grid (whole-image results path); Escape + focus on both new modals; object-URL revoke; PDP look join reuses `getCatalogCardsByHandle` (dropped a 200-product fetch); accent hex is Zod-validated | fix | ✅ `77b60ac`,`09e4b10`,`0d32b57`,`7727343` — typecheck + build green |
| 116 | **Admin toaster sweep** (owner 2026-07-20: "insted of direct save text use toaster for all action"): the shared `useSaveSection` hook now fires success/error toasts; removed every inline "Saved" span + inline save-error span across the 11 section editors, and converted the 2 remaining string-state editors (visual-search mapping, size-guides). All admin saves confirm via the existing toast system | fix | ✅ `6077f6b`,`139f666` |
| 113 | **FN Shop Similar PDP modal** (owner: "FN use individual modal not the global search redirect"): dedicated in-place modal — product photo with garment dots + Upload + size facets on the left, ranked similar-product grid + "Sort by Most similar/price" on the right; runs on the same CLIP engine, dots re-scope by category (top→tops), Upload swaps the query; new card helpers (`similarProductCards`/`similarCardsByVector`, `getCatalogCardsByHandle`) + APIs (`/api/products/similar`, `query/[id]/results`) | add | ✅ `3ddb720`,`16738c1`,`fc521d9`,`5f756be` — E2E: gym-shorts PDP → 24-item grid, Top dot → tops-only |
| 114 | **FN Style-it-with quick-shop modal** (owner's second screenshot): clicking a styled/tagged piece opens a quick-shop modal (image, name, price, See-full-details, color swatches, size grid, Add-to-bag + wishlist) — adds to bag without leaving the PDP via the proven variant→addItem path; reused by the Shop-the-Look dots | add | ✅ `fc521d9`,`5f756be` — verified modal opens with product/price/size UI |
| 115 | **Shop the Look — AI auto-tagging + advanced PDP UI** (owner: "more automation + advance UI"): admin "Auto-detect items" runs the look photo through YOLOS + CLIP to pre-place hotspots and suggest a product per garment (admin confirms); storefront upgraded — numbered dots open the quick-shop modal, a tagged-product strip with per-row Quick-add, and "Add the whole look" (every piece's first variant in one click) | add | ✅ `0bd6f6f`,`15f776e` — auto-detect route admin-gated (401 verified); advanced UI live on the gym-shorts look |
| 109 | **Brand/theme + per-division landing — FIXED** (owner: "brand and theme is generic … different pages render different content"): (1) **division-aware curated landing** — `getLandingData(division)` scopes category tiles + "shop the latest" rails + every href to the division, so `/pages/men` shows men's categories/products (was the women homepage verbatim); (2) **per-division accent** — optional `accentByDivision` hex per division (admin color picker in /admin/site), applied as a `--accent`/`--ring` override on that division's `<main>`; empty = the owner-approved brand claret (default preserved); (3) "Shop by Brand" → "Shop by Department". Full per-division curated hero/promo copy remains best handled via the existing per-division CMS PageLayouts (sections mode, already fully editable) rather than duplicating the whole curated schema | fix | ✅ `ed4806a`,`032cefb`,`eb30b62`,`8c89793` — typecheck + build green |
| 110 | **Visual search UX round 4** (owner 2026-07-19): ONE panel, FN-exact — the Search-By-Image popover now hosts all three states (upload → searching → photo-with-dots) crossfading IN PLACE in the navbar, so it survives the navigation to /search with zero position jump or "page reload" feel; FN highlight = selected garment stays CLEAR, rest of the photo dims (shadow cutout + white ring); search field restyled to FN rounded pill + shadow; panel auto-opens over results (also on shared/refreshed URLs); X on the photo clears the search; new metadata API `GET /api/visual-search/query/[id]` (dots/division, no vectors) | fix | ✅ `546982d`,`503eef0`,`01f2ddd` — visually verified on the owner's gym photo |
| 108 | **Visual search UX round 3** (owner feedback 2026-07-19): (a) FN dot mechanics — the selected garment shows ONLY the translucent highlight box (its dot disappears; tapping the box deselects), other garments keep dots; verified on the owner's gym photo → detects shoes/bottom/top (new smoothing: dress detected alongside bottoms = long-top misread, relabeled), shoes auto-selected like FN; (b) combined-popover flow — query-image thumbnail chip with ✕ inside the search input during an image search, image/discovery popovers same width+position (one unit; camera ⇄ chevron toggles modes); (c) **any-catalog flexibility (owner: future clients may sell only beauty/baby)** — garment→category mapping moved to the admin-editable `visualSearch` SiteSetting (only handles live in the catalog apply; empty→vector-only) and auto-division only applies when the store carries that division; (d) admin clarity — /admin/visual-search rebuilt with "How it works" + mapping editor, /admin/shop-the-look gained a "What this does" explainer | fix+add | ✅ `13f8f34`,`ffb87f8`,`fd3daba`,`6ba8aac`,`a275027`,`49e02a2`,`24af051` — E2E on the owner's photo: bottom dot → Nova Gym Shorts + bottoms only, top dot → tops only |
| 109 | **Brand/theme + division landing audit** (owner: "brand and theme don't serve the purpose — check fully"): audit complete — findings: per-division differentiation only exists in CMS "sections" mode (seed-divisions layouts are genuinely division-scoped); **curated `<Landing>` is women-hardcoded** (`getLandingData()` takes no division; tile hrefs `?division=women` — `/pages/men` in curated mode renders the women homepage verbatim); `site.landing` (hero/promo/tiles) is ONE global blob with no division dimension; **no per-division accent/theme exists** (single global claret token); "Shop by Brand" tiles are division shortcuts mislabeled as brands (one links to a collection instead). Recommended (owner to approve order): (1) division-aware `getLandingData(division)` — kills the biggest defect; (2) `site.landing` keyed per division w/ division selector in /admin/site; (3) per-division accent token (`data-division` → CSS vars); (4) rename/derive "Shop by Brand" → "Shop by Division" or retire | audit | 🟡 findings delivered 2026-07-19; fixes pending owner priority | (`lib/garment-detect.ts`): `onnx-community/yolos-fashionpedia-ONNX` (q8, same self-hosted transformers runtime as CLIP; processor borrowed from Xenova/yolos-tiny — the ONNX repo ships none) detects garments, grouped top/bottom/dress/outerwear/shoes/bag/accessory (max 4), each part CLIP-embedded from a padded crop at upload time → white hotspot dots on the query panel; tapping a dot re-ranks by that crop's vector (`?part=n`, instant — no re-detect). CLIP zero-shot division ("women's fashion/men's fashion/children's clothing", ≥0.5) sets `division=` like FN. All best-effort — detection failure never breaks the search | add | ✅ in `f2d47b8`+`f346d7c` — verified: woman photo → shoes/bottom/accessory dots, division=women; part switch changes result set |

**Round 2 complete: #19–#32 all ✅** (Phases 0–3 + Phase 4 #26/#27 payments/shipping). **#18 (multi-user admin auth + roles) is now built too** — so every tracked task #1–#32 is implemented. ~~Follow-ups before/at deploy: run a cms `db push`…~~ **Done 2026-07-10:** live cms `db push` in sync (`AdminUser`/`AdminRole`, `BRAND_CAROUSEL`, and the 4 #45 `SectionType` enum values all in the production DB), live admin created, and the live catalog + home layout seeded (#36/#37/#42). Only #40's "seed per-division layouts" remains as future work; #39 is an ongoing process rule.

_Verified gap analysis backing these is in this session; build order proposed below
once the architecture is confirmed with the owner._

---

## ✅ Done & deployed
| Area | Notes | Key commit |
|---|---|---|
| Catalog + IA foundation | 6 divisions, content categories, collections, rich product attributes; old demo wiped | `c3dc74d` |
| Listing pages + filters | `/collections/{handle}?division=`, derived facets (Category only on broad; Style-led single-type), Sort/Show/columns, breadcrumb, Tops tile row, per-division sidebar | `72c0a12`, `a193f20`, `0782fbc` |
| Top nav + full-width mega menu | brand swap per division, Sport NEW badge, Men Sale red, broad vs single-type popovers | `51323fa`, `a193f20` |
| Landing page | hero, collab carousel, trend report, promo banner, shop-by-brand, bento, shop-the-latest, rich footer; spacing/full-bleed/popup polish | `1cbe791`, `637dd64` |
| PDP enrichment | materials & care, attributes, tags, reviews (instant), related + trending rows, working share | `a664119`, `990f8ad`, `5c7fac4` |
| Admin product form | division/occasion/style/trend/material/care + richer rich-text editor | `5c7fac4` |
| Device image/video upload | reusable field in product + section editors (storage pluggable) | `4260631` |
| Discount start/expiry dates | `starts_at`/`ends_at` on promotions | `e3d6dd1` |
| Auth | phone-OTP popup, registration (phone+OTP), login by email **or** phone | `e9c638b`, `0c18025` |
| Project `.claude/` setup | agents, commands, skill, docs-sync hook + rules | (committed) |

## 🟡 In progress
- **Phone UX polish** — international phone field (default BD), individual OTP boxes,
  and OTP verify now **auto-creates + logs in** a real customer (derived password).
- **#14 Checkout** — required fields (email, phone, first name, address-by-landmark,
  city), country auto-by-location, intl phone field, persona section + stacked 2–4% discount.
- **#15 Admin persona builder** — dynamic title/bracket text/questions/discount % (in `SiteSetting`).

## ⬜ Planned (next)
- **#7 Re-CMS-ify the landing + Site Settings UI** — make every landing block, the
  announcement bar, marquee, brand names, **delivery line**, and **size guide** editable in `/admin`.
- **#12 Checkout shipping/payment** — verify end-to-end; surface/explain creation
  (Medusa admin) vs custom admin.

## ⏸️ On hold — decisions deferred (see `PRODUCTION_DECISIONS.md`)
- **Image/video upload storage backend** (R2 / Cloudinary) — currently Medusa local disk (ephemeral on free tier).
- **SMS/OTP gateway** — currently mocked (demo code shown on screen).
- **Production hosting** (server + DB) — owner to plan; currently Neon + Render-free + Vercel.

---

_Last updated: 2026-07-10 — live go-live sync: production CMS schema + admin (#38), live catalog & home seed (#36/#37/#42)._
