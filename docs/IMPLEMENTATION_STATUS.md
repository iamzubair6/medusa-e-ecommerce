# Implementation Status — Fashion-Nova rebuild (living doc)

Single source of truth for what's **done**, **in progress**, and **on hold**.
Update this with every change (see CLAUDE.md "Keep docs in sync"). Older platform
phases (0–5) live in `ROADMAP.md`; deferred production choices in
`PRODUCTION_DECISIONS.md`.

Legend: ✅ done & deployed · 🟡 in progress · ⬜ planned · ⏸️ on hold (needs a decision)

---

## Task board (the 15 tasks) — durable record

> Mirror of the working task list so it persists in the repo. Keep statuses current.

| # | Task | Status |
|---|---|---|
| 1 | Catalog + IA data foundation (6 divisions, categories, collections, attributes) | ✅ |
| 2 | Fix left-sidebar category bug (per-division) | ✅ |
| 3 | Top nav + full-width mega menu (broad vs single-type) | ✅ |
| 4 | Listing pages + filters (`/collections?division=`, derived facets, toolbar) | ✅ |
| 5 | Landing page rebuild (hero, trend, bento, shop-the-latest, footer) | ✅ |
| 6 | Enrich PDP (details, materials, reviews, related/trending) | ✅ |
| 7 | Admin UX overhaul — Site Settings + CMS-managed landing + shadcn fields | ✅ Site Settings `e787ca9`; CMS landing + dynamic add/reorder `79dd28b`; shadcn Combobox + DatePicker `5d8d002` |
| 8 | Device image/video upload field (storage pluggable) | ✅ |
| 9 | Storefront bugs — product share + instant reviews | ✅ |
| 10 | Admin product form — division/occasion/style/trend/material/care + richer editor | ✅ |
| 11 | Discounts — start/expiry dates | ✅ |
| 12 | Checkout — shipping methods + payment options (verify + surface) | ✅ `d8c392d` — BD shipping ৳60 + COD verified; shipping editable in admin, payment card added |
| 13 | Auth — phone-OTP popup, registration (phone+OTP), email/phone login, intl phone, boxed OTP, auto-login | ✅ |
| 14 | Checkout — required fields + intl phone + prefill + persona section + stacked discount | ✅ `d5daa73` |
| 15 | Admin — persona builder (title/bracket/questions/promo code) | ✅ `b876f5b` |
| 16 | Required-field red star (*) across storefront + admin forms | ✅ `de2c591` product/discount/auth/reviews/phone/checkout (admin `required` prop available for any field) |
| 17 | Fix Vercel build fail — time-box sitemap product fetch (cold Medusa hung past 60s) | ✅ `32fc21b` |
| 18 | Multi-user admin auth + roles (replace single-password `ADMIN_PASSWORD` gate) | ✅ built (typecheck + build green; pending commit/deploy) — CMS `AdminUser` + `AdminRole` (ADMIN/EDITOR), scrypt-hashed passwords, HMAC-signed session cookie (`lib/session.ts`, edge-safe), `/admin/users` Team & Roles manager, role-gated middleware; first-run bootstraps an ADMIN from `ADMIN_BOOTSTRAP_EMAIL` + `ADMIN_PASSWORD` then retires it |

**Done: 18/18.** Multi-user admin auth + roles (#18) is implemented; see `docs/SMOKE_TEST_18-32.md` for the visual verification checklist. Follow-up: production needs a cms `db push` (adds `AdminUser`/`AdminRole`) — same as the `BRAND_CAROUSEL` enum note below.

> Full day-by-day history of the whole project: [BUILD_LOG.md](./BUILD_LOG.md).

---

## Round 2 — Fully dynamic, Shopify-style CMS (2026-06-09 feedback) ⬜ planned

> **Vision:** every part of the storefront — navigation/IA, mega-menu, landing,
> listing config, PDP content, shipping/payment, settings — must be **admin-managed
> and dynamic** (no hardcoding), like Shopify/other ecommerce CMS. The IA follows
> Fashion Nova: **Division (page `/pages/{division}`) → Collection (mega-menu item
> w/ multi-column popover) → Category (listing filter / special section)**.

| # | Task | Type | Status |
|---|------|------|--------|
| 19 | Restore curated landing (homepage no longer prefers stale seeded CMS home) | fix | ✅ `3e50050` |
| 20 | CMS-manage full landing — editable blocks | add | ✅ `c70705e` hero/promo/feature/sale + trend-card, brand-tile & **collab carousel slides** all editable (`landing.collabSlides` + `CollabSlideArray` editor); `/` renders a PUBLISHED `home` PageLayout via `SectionRenderer` when it has sections, else curated `<Landing>` (#19-safe); optional `BRAND_CAROUSEL` SectionType (schema+renderer) — **needs cms `db push` to add the enum to the DB before a BRAND_CAROUSEL section is created**. Prior: `a2dfd74`,`4da9987` |
| 21 | Section manager: add Create + Delete sections (dynamic) | add | ✅ `(phase1)` create/delete/reorder/edit live |
| 22 | Divisions as admin-managed pages `/pages/{division}` (underline active; per-division mega) | add | ✅ `fd226e8`,`71e9fa1` routing + underline + admin labels/badges (per-division landing content = future) |
| 23 | Admin collection → multi-column popover builder (data-driven mega menu) | add | ✅ `71e9fa1` /admin/navigation builds divisions→collections→columns→links; navbar renders it (fallback to auto) |
| 24 | Listing: generalize category checkboxes + special sub-type section (admin-config) | update | ✅ `listingConfig` SiteSetting + `/admin/listings`: per-listing Category facet show/hide, filter-group reorder, and curated tile row (any source/limit) — replaces the hardcoded Tops row |
| 25 | Migrate remaining admin SelectField → Combobox | update | ✅ `c9bb0d7` all section editors + price-list (incl. searchable category/collection) + campaign + product-creator now use Combobox; `EnumCombobox` helper narrows to the option union (no casts) |
| 26 | Dynamic payment methods management in admin | add | ✅ `a3c7437` `/admin/payments` — CMS `checkout` SiteSetting `paymentMethods[]` (id/label/description/enabled), Zod-validated, checkout renders enabled methods; `/complete` validates chosen method. Live Medusa providers shown read-only (BDT region). |
| 27 | Dynamic shipping methods/zones management in admin | add | ✅ `a3c7437` `/admin/shipping` — live Medusa option amounts (edit via Admin API) + per-option zone display + CMS `checkout.shippingMethods[]` note/visibility override; checkout shipping list filters hidden + shows notes. |
| 28 | Fix hero slide editor design/alignment issue | fix | ✅ `3e50050` |
| 29 | Product form: richer material & care (rich text) | update | ✅ `3e50050` |
| 30 | Move persona into checkout contact-info step | update | ✅ `3e50050` |
| 31 | PDP accordions: max-height + scroll | fix | ✅ `3e50050` |
| 32 | Admin-editable Shipping & Returns content | add | ✅ `3e50050` |
| 33 | Automated test setup — Vitest (unit/component) + Playwright (e2e) | infra | ✅ `b304366` — root `vitest.config.ts` (jsdom + RTL, React 19, `@/`→apps/web) + `vitest.setup.ts`; sample tests for `cn`, `Button`, `parseSiteSettings` (9 passing); Playwright `apps/web/playwright.config.ts` + `e2e/home.spec.ts` smoke (auto-starts `next dev` :3200, passes on placeholder data; 1 passing). Scripts: `test`, `test:watch`, `test:e2e`, `test:e2e:install`. Test files excluded from `tsc`/`build` (typecheck green). |
| 34 | `createsuperuser`-style admin CLI (`admin:create`) | infra | ✅ `9926671` — `packages/cms/scripts/create-admin.ts` inserts an admin straight into the CMS `AdminUser` table (reuses `createAdminUser` scrypt hashing); interactive (hidden password) **and** flag-driven (`--email/--name/--role/--password`), plus read-only `--list`. Auto-loads `CMS_DATABASE_URL` from `apps/web/.env`. Removes the need for the `ADMIN_BOOTSTRAP_EMAIL`/`ADMIN_PASSWORD` first-run dance. Run: `bun run admin:create`. Fixed via `74a07c0` (run through `bun` directly so interactive TTY works). |
| 35 | Admin sidebar: pin to viewport (was stretching with tall content → bottom whitespace) | fix | ✅ `b67d973` — `components/admin/sidebar.tsx`: `<aside>` now `sticky top-0 h-screen self-start`; nav `overflow-y-auto`. |
| 36 | CMS-driven home: build the published `home` PageLayout into a full Fashion-Nova composition (owner chose CMS-driven over curated `<Landing>`) | add | ✅ LOCAL — published `home` rebuilt to **10 sections** in Fashion-Nova order: `MARQUEE → HERO → VALUE_PROPS → PROMO_SPLIT → PRODUCT_ROW → TREND_RAIL → CATEGORY_GRID → COUNTDOWN → BRAND_CAROUSEL → BANNER` (idempotent `packages/cms/prisma/seed-home.ts`, `bun run --filter @ecom/cms db:seed-home`; only the `home` PageLayout is touched — nav/popups/site-settings untouched). Uses the 4 new section types from #45. **Live seeded 2026-07-10** (same 10 sections, via `seed-home.ts` against live). |
| 37 | Seed complete, realistic data (Medusa catalog: products/variants/prices/categories/collections + CMS content: home sections/nav/popups) so the site looks production-real | add | ✅ local + **LIVE seeded 2026-07-10** (owner-authorized) — `seed-fashion-catalog` against the live Medusa DB: 26 products, 18 categories, 4 collections, 248 inventory levels, BDT region, color×size variants, multi-currency prices, offers. Live Store API verified (`count: 26`). Helper: `scripts/seed-live-catalog.sh` (`1897539`). |
| 38 | Live login 500 — live Neon CMS DB missing `AdminUser`/`AdminRole` (P2021) | fix | ✅ resolved 2026-07-10 — live Neon CMS schema in sync (`prisma db push` reports no drift; `AdminUser`/`AdminRole` + all `SectionType` enum values present), live admin exists (`admin@gmail.com`, ADMIN, active), live `/admin/login` returns 200. |
| 39 | Process rule: every backend/frontend change must be applied to BOTH local and live (schema pushes, seeds, deploys) | infra | 🟡 ongoing — captured per owner instruction 2026-06-14. |
| 40 | Unify landing model → **CMS sections everywhere** (owner decision): make `/pages/[division]` render CMS PageLayout sections (not the curated `<Landing>`), and enrich section types to reach Fashion-Nova richness (trend-card grid, brand tiles, collab carousel, richer hero) | add | 🟡 step 1 done — `/pages/[division]` renders a published PageLayout whose slug = division handle via `SectionRenderer`, falling back to curated `<Landing>` when none. **Step 2 done (#45):** richness enrichment — 4 new section types (PROMO_SPLIT, TREND_RAIL, VALUE_PROPS, COUNTDOWN) added end-to-end + seeded into `home`. **Next:** seed per-division layouts. |
| 45 | Enrich CMS section types to Fashion-Nova richness — **4 new SectionTypes end-to-end** | add | ✅ LOCAL — **PROMO_SPLIT** (2/3-up promo image cards w/ overlay + CTA), **TREND_RAIL** (horizontal snap rail of tall labeled cards), **VALUE_PROPS** (trust-badge strip; icon key→lucide), **COUNTDOWN** (live ticking urgency banner, client component, falls back to heading when elapsed). Each: Zod schema in `packages/cms/src/schemas/sections.ts` (+ `sectionConfigSchemas` + `defaultSectionConfig`), `SectionType` enum value, storefront component in `apps/web/components/site/` wired into `section-renderer.tsx`, admin editor in `apps/web/components/admin/editors/` wired into `section-manager.tsx` (creatable list is `SECTION_TYPES`-derived, so auto-listed). Local cms `db push` added the 4 enum values + `prisma generate`. typecheck (all packages) + `bun run build` green. Live cms `db push` done 2026-07-10 — the 4 enum values (and `AdminUser`/`BRAND_CAROUSEL`) are in the production DB. |
| 41 | Phone-capture popup → **dedicated `/admin` screen** + dynamic text (owner decision): make title/body/button/success/enabled editable (currently hardcoded in `phone-capture-popup.tsx`), wire the popup to read it | add | ✅ — new `phoneCapture` SiteSetting + Zod schema (`lib/phone-capture-settings.ts`), `/admin/phone-popup` editor (RHF+Zod) saving via `POST /api/admin/phone-capture`, sidebar "Phone Popup" item, popup reads managed copy & `enabled=false` hides it. `db push` not required (uses existing `SiteSetting` table). |
| 46 | Fix Vercel build fail — `/cart` (+ other per-user pages) static-prerender hung on cold Medusa via `getNavData`/`SiteNavbar` (>60s timeout, like #17) | fix | ✅ `export const dynamic = "force-dynamic"` on `/cart`, `/track`, `/wishlist`, `/checkout/success` so they render at request time (not prerendered at build). **Root fix:** `medusaFetch` now time-boxes every Store API call (`AbortSignal.timeout(10s)`) — a cold Render backend *hangs* the connection (never errors) so the existing try/catch never fired; on timeout it aborts → returns null → callers fall back to placeholders, ISR fills real data once warm. This protects all ISR/static pages (`/` fell over next). Local `bun run build` → 49/49 static pages, green. |
| 42 | Seed: relatable, per-category product images (was rotating a generic Unsplash pool → unrelated photos); re-seed local | fix | ✅ LOCAL — `seed-fashion-catalog.ts` now uses a category-keyed `IMAGE_SETS` map (`imageSetKey`/`pairFor`); re-seeded, verified per-type (jacket→outerwear, dress→dresses, heels→shoes, kids→kids, beauty→beauty). **Live re-seeded 2026-07-10** (part of the #37 live seed). |
| 43 | Fix `/admin/listings` duplicate-key React error (division handle collides with same-named category handle after full seed) | fix | ✅ `app/admin/(panel)/listings/page.tsx` dedupes `targets` by handle (division wins). typecheck green. |
| 47 | Process rules (owner, 2026-07-10): no co-author lines in commits; 2–3 files per commit; status doc updated with each task; run pipeline agents properly; read code before changing; read docs at session start; R&D per task; ask when confused; minimal replies; repo docs = memory (`docs/LESSONS.md` added as do-not-repeat log) | infra | ✅ codified in `CLAUDE.md` + `docs/LESSONS.md` |
| 48 | Process rules pt.2 (owner, 2026-07-10): senior-developer mindset — standard practice, stay current with the stack, DRY/KISS/YAGNI/SOLID in all code, and proactively suggest better alternatives when the owner's approach is suboptimal (honest push-back before building) | infra | ✅ codified in `CLAUDE.md` ("Senior-engineer mindset") |
| 49 | Footer audit → CMS content pages: 9 footer links 404'd (`/help`, `/help/size-guide`, `/help/shipping`, `/help/returns`, `/about`, `/careers`, `/contact`, `/sustainability`, `/gift-cards`) + no legal pages. Built admin-managed content pages: `contentPages` SiteSetting (Zod, list of slug/title/body/enabled), root catch-all `app/[...slug]` renders them (11 seeded defaults incl. new `/privacy-policy` + `/terms`, linked in footer), `/admin/content-pages` editor (RHF+Zod, add/remove/hide pages, rich text). No `db push` needed. | add | ✅ `7f35f44`,`783a1e3`,`26b5c20` — typecheck+build green; 11 routes smoke-tested 200 / unknown 404; code-reviewed (fixed: revalidatePath on save, empty-list vs never-saved defaults, server-side HTML sanitize, reserved/duplicate-slug validation, React.cache, a11y + delete confirm) |
| 50 | Real transactional email via **Brevo** free tier (300/day): OTP delivered to the register email when `BREVO_API_KEY`+`EMAIL_FROM` set (devCode fallback otherwise), order-confirmation email on checkout complete (best-effort, never blocks the order) | add | ✅ `9c27ab5`,`15942d6` — code-reviewed & hardened: OTP delivery bound to the account's stored email (anti-takeover), `devCode` needs `OTP_DEMO_CODES=true` in prod, IP+phone rate limits. Owner setup = smoke-tests/50 |
| 51 | **SSLCommerz sandbox** online payment: "Pay Online — bKash / Card" checkout method → hosted gateway redirect (`/api/checkout/pay`), validator-API-verified callback completes the order (amount+cart matched; cart cookie not trusted cross-site), failed/cancelled banners, `/complete` refuses gateway method | add | ✅ `ee56a71`,`9d2f282`,`05d4873` — code-reviewed: validator-only trust, amount+currency+cart binding, IPN for browser-loss, cart-id shape check, replay-safe. Owner setup = smoke-tests/51 |
| 52 | **Shop the Look** (FN outfit tagging): `/admin/shop-the-look` click-to-tag editor (hotspot % coords + linked product, max 8/look, one look/product, `shopTheLook` SiteSetting), PDP renders numbered pulsing dots (server component, reduced-motion safe) linking each piece | add | ✅ `745077c`,`4473d01`,`a512b97` — focus-visible dots, revalidates affected PDPs on save; no `db push` needed |
| 53 | Per-division landing PageLayouts: `seed-divisions.ts` (+`db:seed-divisions`) rebuilds 6 published layouts — women/men/**plus**(=Curve route alias)/sport/kids/beauty, 5-6 sections each, Zod-validated, home untouched | add | ✅ `4235c6e` — seeded local + **LIVE 2026-07-10** (owner-authorized); all 6 live division pages verified 200 |
| 54 | Polish trio: navbar **search autocomplete** (debounced TanStack Query → `/api/search` proxy, combobox a11y), **branded 404** (`app/not-found.tsx`), **keep-warm** GH Actions ping every 30 min to Render `/health` | add/fix | ✅ `888b935`,`0efb3ed` — reviewed: option-role a11y fix, `/api/search` rate-limited, keep-warm at 10 min (UptimeRobot recommended long-term); `170cbe6` honest search-error row |
| 55 | Follow-up: auth form converted to RHF + Zod (was the last raw-useState form; per-field validation messages) | update | ✅ `5011afe` (superseded by #57 split) |
| 56 | Register 500→401 bug: local Medusa was down → `registerCustomer` threw (bare 500) AND the OTP was consumed, so retry hit 401. Fixed: `verifyOtp` retry-safe (correct code stays valid until expiry), register route try/catch → friendly 502 | fix | ✅ `72e3011` — E2E verified incl. retry-after-failure |
| 57 | Auth redesign (owner feedback 2026-07-11): **separate** `/account/login` (minimal card) and `/account/register` (editorial split-panel) pages; register is **2-step** — details → **Next** (auto-sends code) → verify screen with 6-box input, masked destination, 30s resend cooldown, "edit details" back-link; **OTP now 6 digits everywhere** (generator, verify APIs, boxes, phone popup + its default copy) | update | ✅ `cf46e7e`,`6a549f8`,`f2d9f72`,`410d867`,`e957906` + `228b1d0`,`9766dcf` (**standalone layouts** — auth pages render without site navbar/footer: logo + "Back to store" only) — E2E: 6-digit devCode, register 201, both pages 200, zero `<footer>`/navbar markup |
| 44 | Landing-style toggle → resolve the "two landing systems" inconsistency with an explicit **per-page** choice (admin) between the curated Fashion-Nova `<Landing>` and the CMS section layout, for the home page + each division | add | ✅ — new `landingMode` SiteSetting + Zod schema (`lib/landing-mode.ts`: record page-key→`"curated"\|"sections"`, `landingModeFor` defaults to `"sections"`), `/admin/landing-style` editor (RHF+Zod, one segmented "CMS Sections" vs "Fashion-Nova Landing" toggle per page) saving via `POST /api/admin/landing-mode`, sidebar "Landing Style" item (`LayoutPanelTop`). `app/page.tsx` + `app/pages/[division]/page.tsx` now read `landingMode` and gate: `"sections"`→published PageLayout when it has sections else curated `<Landing>` (= prior behavior); `"curated"`→always `<Landing>`. No `db push` (uses existing `SiteSetting` table). typecheck + `bun run build` green. |

| 58 | Local checkout `/api/checkout/shipping` 502: Medusa `list-shipping-options-for-cart` crashed (`undefined.fulfillment_sets`) because the sales channel kept a **live link to a soft-deleted stock location** ("European Warehouse", deleted 2026-06-02, link not cascaded). Data fix: soft-deleted the stale `sales_channel_stock_location` row in the local DB; verified shipping returns ৳60/৳120 options. Live DB checked (read-only) — clean, no dangling links. No code change. | fix | ✅ data-only (local DB) 2026-07-11 |
| 59 | Professional order numbers: branded `MSN-00042` format via `lib/order-id.ts` (`formatOrderId`/`parseOrderId`) — used in all emails, checkout success, track page + lookup (API accepts "MSN-00042"/"#42"/"42"), admin orders list/detail/customer history | update | ✅ built 2026-07-11 |
| 60 | Emails/gateway links use the REAL request origin (`lib/origin.ts`: x-forwarded-host → host → url; localhost→http) — track links point at :3200 locally / live domain in prod; SSLCommerz callbacks no longer hit the wrong port (`.env` NEXT_PUBLIC_SITE_URL also fixed 3000→3200) | fix | ✅ built 2026-07-11 |
| 61 | Track page redesign: grid-column timeline (labels exactly under icons — old markup nested connectors inside steps), Ordered→Processing→Shipped→Delivered with dates, claret current-step ring, brass-rule result card, parcel links, empty/error states | update | ✅ built 2026-07-11 |
| 62 | Local Medusa backend admin login: `admin@maison.local` / `maison2026` at http://localhost:9000/app (created via `npx medusa user`) | infra | ✅ 2026-07-11 |
| 64 | Admin sidebar organized into labeled groups: Overview / Sales / Catalog / Content / Marketing / System (all items + roles preserved; Email Templates added under Content) | update | ✅ built 2026-07-11 |
| 65 | `/account` profile redesign: monogram header, personal-details card (now RHF+Zod), order history table/cards with branded IDs + status badges, address book grid — all via TanStack Query, same endpoints | update | ✅ built 2026-07-11 |
| 67 | Admin-editable **email templates** (`/admin/email-templates`): 5 branded templates (OTP, order confirmation, shipped, delivered, welcome) with subject/heading/body editing, placeholder legend, **send-test-to-inbox** (previews unsaved edits); fixed on-brand shell (ink band + claret rule + parchment card) in code; senders wired: otp/request, register (welcome), checkout complete + SSLCommerz callback (confirmation), ship (shipped + tracking no.), deliver (delivered). Stored in `emailTemplates` SiteSetting — no `db push`. | add | ✅ built 2026-07-11 — E2E: COD order MSN-00006 sent the templated confirmation |
| 68 | Footer content pages redesigned: hero band (eyebrow + oversized Fraunces title + brass hairline), styled body typography (standfirst first paragraph, hairlined h2s, brass list markers, claret links), closing contact CTA; richer default copy | update | ✅ built 2026-07-11 |
| 69 | **Real SMS OTP via MiMSMS** (owner purchased credit 2026-07-12): `lib/otp-sms.ts` implements the V2 transactional API (`POST api/V2/SMS`, type T, BD msisdn normalization, 15s timebox, throws surfaced errors). Env: `SMS_PROVIDER=mimsms` + `MIMSMS_API_KEY`/`MIMSMS_USERNAME`/`MIMSMS_SENDER_ID`. With SMS live, demo codes stay off and the phone-capture popup verifies for real. | add | 🟡 code ready; awaiting owner's panel username + sender ID + IP/domain whitelist to activate & live-test |
| 76 | **SMS relay on Render** — MiMSMS rejected dynamic-IP whitelisting (owner's ISP rotates IPs; Vercel has none fixed). New Medusa route `POST /sms` (secret-header auth, BD-number + size guards) relays to MiMSMS from Render's **static outbound IPs**; `lib/otp-sms.ts` dispatches via `MIMSMS_RELAY_URL`+`SMS_RELAY_SECRET` when set (direct mode kept for static hosts). Owner setup: Render env (MIMSMS_*, SMS_RELAY_SECRET) + whitelist Render outbound IPs + domain in MiMSMS panel. | fix | 🟡 built; awaiting Render env + MiMSMS whitelist |
| 70 | Phone popup OTP is 6-digit — confirmed already shipped with #57 (OtpInput default 6, popup validates 6) | verify | ✅ (no change needed) |
| 71 | Registration OTP **channel choice**: "Send my verification code by — SMS to my phone / Email" segmented picker; `/api/otp/request` honors `channel` ("sms" skips email — always safe, the SMS goes to the phone being verified); response reports channel + masked destination ("texted a 6-digit code to +8801•••••123") | add | 🟡 built (typecheck green; pending commit) |
| 73 | **Order confirmation SMS alongside email** (COD complete + SSLCommerz callback): `sendTransactionalSms` to the order phone — "Maison: order MSN-000XX confirmed (Tk …). Track: …" (phone captured pre-completion; best-effort, never blocks). Bulk foundation: `sendPromotionalSms` (MiMSMS type P, 1000/batch, dedupe) powering #72 | add | 🟡 built (typecheck green; pending commit) |
| 63 | `/admin/shipping` dynamic zones + options: zone cards (name + ISO-2 country chips, RHF+Zod edit/create), per-zone "Add shipping option" (name + flat ৳, mirrors seeded manual_manual/flat/BDT-region shape — payloads verified against the live local Medusa v2 API), delete option, delete empty zone. New helpers in `lib/medusa-admin.ts` (`listShippingZones`/`createShippingZone`/`updateShippingZone`/`deleteShippingZone`/`createShippingOption`/`deleteShippingOption`; zones read via `GET /admin/stock-locations` — v2 has no `GET /admin/fulfillment-sets`); routes under `/api/admin/shipping-options{,/[id],/zones,/zones/[zoneId]}` (Zod-validated, middleware-gated). CMS note/visibility overrides preserved. | add | ✅ built 2026-07-11 (typecheck + build green; pending commit) |
| 66 | `/admin/orders/[id]` order-status management surfaced: vertical status stepper (Ordered → Processing → Shipped → Delivered, timestamps + "Next" marker, canceled state) with ONE primary action for the next step — Start processing (fulfil) → Mark shipped (RHF+Zod tracking-number form) → Mark delivered — plus Cancel until shipped; each action toasts + refreshes. `packedAt` now mapped onto `AdminFulfillment`. | update | ✅ built 2026-07-11 (typecheck + build green; pending commit) |
| 75 | **Steadfast Courier integration (test-mode) + order handover**: `lib/steadfast.ts` env-driven client (`STEADFAST_API_KEY`/`STEADFAST_SECRET_KEY`, optional `STEADFAST_BASE_URL`, default `https://portal.packzy.com/api/v1`; headers `Api-Key`/`Secret-Key` — payloads verified against the official steadfast-it Laravel package source): `createConsignment` (POST `/create_order`), `getConsignmentStatus` (GET `/status_by_cid/:id`), Zod-validated, 15s timeboxes, status mapping pending→"Handed to courier" / in_review·hold→"In transit" / delivered\*→"Delivered" / cancelled\*→"Returned/Cancelled" (raw kept). `/admin/orders/[id]` Ship step now offers **Send to Steadfast** (only when configured; creates consignment with invoice=`MSN-XXXXX`, COD amount for COD orders, then normal `shipOrder` flow → shipped email + `steadfast.com.bd/t/<code>` tracking link on `/track`) alongside the manual tracking form; consignment stored in order metadata (`setOrderMetadata`); "Courier: Steadfast" card shows consignment id, tracking link + live status via TanStack Query (GET `/api/admin/orders/[id]/steadfast`, refresh button). No keys → option hidden, manual flow untouched. | add | 🟡 built 2026-07-12 (typecheck green; uncommitted; awaiting owner's Steadfast keys to live-test) |
| 72 | `/admin/customers` marketing tools: **promo SMS composer** (RHF+Zod; 160-chars/segment GSM vs 70 Bengali/Unicode with live segment count; audience = "all with a phone" or checkbox-selected rows; typed `SEND <count>` confirmation with ~৳0.35/SMS/segment cost estimate; POST `/api/admin/customers/sms` resolves phones server-side — "all" paginated from Medusa, "selected" by customer IDs only — then `sendPromotionalSms`; "SMS is not configured." surfaces as a banner) + **CSV export** (GET `/api/admin/customers/export` streams Name/Email/Phone/Joined/Orders for all customers, 500/page capped at 10k, quote-escaped + formula-injection-safe, `customers-YYYY-MM-DD.csv`) + row checkboxes w/ select-all-on-page (phone-less rows disabled); `AdminCustomerRow` now carries `phone`, table shows a Phone column | add | ✅ built 2026-07-12 (typecheck green; pending commit) |
| 74 | **Bulk product upload via Excel** (`/admin/products/import`, sidebar Catalog → Import Products): GET `/api/admin/products/import/template` generates an .xlsx (exceljs — owner-approved dep) with a "Products" sheet (styled/frozen header, 2 skipped EXAMPLE rows, 200 template rows, dropdown data-validations for division/category/collection/size-set/offer-type sourced from a hidden Lists sheet with LIVE category+collection handles, whole-number checks on prices/percent) + an "Instructions" sheet explaining every column; POST `/api/admin/products/import?mode=preview\|commit` (multipart `file`, caps 200 rows/5MB) parses with defensive cell coercion (`lib/product-import.ts`), validates every row against `productSchema` + live handle maps with per-field messages, preview returns green/red row table, commit creates valid rows sequentially via `createProduct` (now supports `collectionId`→`collection_id`) and reports per-row created/failed. One row = one product; comma-separated multi-value cells; swatches auto-derived from color names. | add | ✅ built 2026-07-12 (typecheck green; E2E verified locally: template → fill 2 rows → preview 2 valid → commit created `prod_01KX9GVR31ED5H39VXEKR2S26M` + `prod_01KX9GVRAX4JXKHT1Y3R51HT1T` with full metadata/variants/collection → deleted; catalog back to 26; uncommitted) |

**Round 2 complete: #19–#32 all ✅** (Phases 0–3 + Phase 4 #26/#27 payments/shipping). **#18 (multi-user admin auth + roles) is now built too** — so every tracked task #1–#32 is implemented. ~~Follow-ups before/at deploy: run a cms `db push`…~~ **Done 2026-07-10:** live cms `db push` in sync (`AdminUser`/`AdminRole`, `BRAND_CAROUSEL`, and the 4 #45 `SectionType` enum values all in the production DB), live admin created, and the live catalog + home layout seeded (#36/#37/#42). Only #40's "seed per-division layouts" remains as future work; #39 is an ongoing process rule.

_Verified gap analysis backing these is in this session; build order proposed below
once the architecture is confirmed with the owner._

---

## ✅ Done & deployed
| Area | Notes | Key commit |
|---|---|---|
| Catalog + IA foundation | 6 divisions, content categories, collections, rich product attributes; old demo wiped | `c3dc74d` |
| Listing pages + filters | `/collections/{handle}?division=`, derived facets (Category only on broad; Style-led single-type), Sort/Show/columns, breadcrumb, Tops tile row, per-division sidebar | `72c0a12`, `a193f20`, `0782fbc` |
| Top nav + full-width mega menu | brand swap per division, Sport NEW badge, Men Sale red, broad vs single-type popovers | `51323fa`, `a193f20` |
| Landing page | hero, collab carousel, trend report, promo banner, shop-by-brand, bento, shop-the-latest, rich footer; spacing/full-bleed/popup polish | `1cbe791`, `637dd64` |
| PDP enrichment | materials & care, attributes, tags, reviews (instant), related + trending rows, working share | `a664119`, `990f8ad`, `5c7fac4` |
| Admin product form | division/occasion/style/trend/material/care + richer rich-text editor | `5c7fac4` |
| Device image/video upload | reusable field in product + section editors (storage pluggable) | `4260631` |
| Discount start/expiry dates | `starts_at`/`ends_at` on promotions | `e3d6dd1` |
| Auth | phone-OTP popup, registration (phone+OTP), login by email **or** phone | `e9c638b`, `0c18025` |
| Project `.claude/` setup | agents, commands, skill, docs-sync hook + rules | (committed) |

## 🟡 In progress
- **Phone UX polish** — international phone field (default BD), individual OTP boxes,
  and OTP verify now **auto-creates + logs in** a real customer (derived password).
- **#14 Checkout** — required fields (email, phone, first name, address-by-landmark,
  city), country auto-by-location, intl phone field, persona section + stacked 2–4% discount.
- **#15 Admin persona builder** — dynamic title/bracket text/questions/discount % (in `SiteSetting`).

## ⬜ Planned (next)
- **#7 Re-CMS-ify the landing + Site Settings UI** — make every landing block, the
  announcement bar, marquee, brand names, **delivery line**, and **size guide** editable in `/admin`.
- **#12 Checkout shipping/payment** — verify end-to-end; surface/explain creation
  (Medusa admin) vs custom admin.

## ⏸️ On hold — decisions deferred (see `PRODUCTION_DECISIONS.md`)
- **Image/video upload storage backend** (R2 / Cloudinary) — currently Medusa local disk (ephemeral on free tier).
- **SMS/OTP gateway** — currently mocked (demo code shown on screen).
- **Production hosting** (server + DB) — owner to plan; currently Neon + Render-free + Vercel.

---

_Last updated: 2026-07-10 — live go-live sync: production CMS schema + admin (#38), live catalog & home seed (#36/#37/#42)._
