# Test #59–#68 — order IDs, emails, track/account/admin redesigns

One checklist for the whole round. No new signups needed (Brevo + SSLCommerz
from #50/#51 keep working).

## #59+#60 — Branded order numbers + correct links

- [ ] Place any order → success page shows **MSN-000XX** (not #5).
- [ ] Confirmation email shows the same MSN number, and its **Track your order**
      button opens the site you ordered on (localhost:3200 locally, live domain
      on live).
- [ ] `/track` finds the order whether you type `MSN-00006`, `#6`, or `6`.
- [ ] `/admin/orders` list + detail show MSN numbers.

## #61 — Track page

- [ ] `/track` looks premium; after lookup the timeline reads Ordered →
      Processing → Shipped → Delivered with each label **directly under its
      icon**, done steps ink-filled with dates, current step ringed in claret.

## #62 — Local Medusa admin

- [ ] http://localhost:9000/app logs in with `admin@maison.local` / `maison2026`.

## #63 — Shipping zones (admin)

- [ ] `/admin/shipping` shows zone cards with country chips.
- [ ] **Add zone** (e.g. "India" with `IN`) → appears; **Add shipping option**
      inside it (e.g. "India Standard", ৳500) → appears at checkout for that
      zone's addresses; delete the option, then the empty zone.

## #64 — Admin sidebar

- [ ] Sidebar is grouped: Overview / Sales / Catalog / Content / Marketing /
      System, with an "Email Templates" item under Content.

## #65 — Account profile

- [ ] `/account` (signed in): monogram header, tidy personal-details card,
      order history with MSN numbers + status badges, address book grid.

## #66 — Order status (admin)

- [ ] Open `/admin/orders/<order>`: status stepper shows the current stage with
      ONE highlighted next-step button — **Start processing** → **Mark shipped**
      (asks tracking number) → **Mark delivered**. Cancel available until shipped.
- [ ] After "Mark shipped": customer gets the **shipped email** with the
      tracking number; `/track` shows Shipped.
- [ ] After "Mark delivered": **delivered email**; `/track` shows Delivered.

## #67 — Email templates (admin)

- [ ] `/admin/email-templates`: 5 templates listed; edit any subject/body.
- [ ] **Send test** delivers a sample (with dummy MSN-00042 data) to the inbox
      you set — including your UNSAVED edits.
- [ ] Save → the next real email uses your edited copy.

## #68 — Footer pages

- [ ] `/about`, `/help/shipping`, `/privacy-policy` etc. now open with a big
      editorial hero title, styled headings/lists, and a "Contact us" strip at
      the bottom (absent on `/contact` itself).
