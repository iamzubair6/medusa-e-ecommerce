# Tests #144–#147 — Round 8: stock alerts, PDP alignment, restock centre, saved pieces, master email

## 1. Stock alerts (#144)
- [ ] Set any size's stock to 0 in the product editor → /admin dashboard shows a
      **Stock alerts** section with that item FIRST, a red "Sold out" badge and a
      "N sold out" count in the heading; clicking the name opens the editor.
- [ ] Items at 1–5 show "N left" (accent). Restock the item → it drops off.

## 2. PDP notify + wishlist (#145)
- [ ] On a sold-out size, the notify-me block sits in a framed card and the
      heart button is vertically centered next to it (no more top-pinned heart).

## 3. Restock centre (#146)
- [ ] /admin/restock: LEFT "Sold out & low stock" (works with zero subscribers —
      that's the admin early-warning), RIGHT "Customers waiting" with counts and
      the same one-click Notify. Both have sane empty states.

## 4. Saved pieces (#146)
- [ ] Heart 2–3 products → /wishlist shows the editorial grid (no flash of the
      empty state on load); removing a piece animates out; with reduced motion
      enabled nothing animates.
- [ ] Empty wishlist shows "Nothing saved yet." with a link to New In.

## 5. Master email template (#147)
- [ ] /admin/email-templates → "Your templates" contains **Maison master
      (announcement)**; the preview shows the full design WITHOUT the default
      shell (it's a complete HTML document).
- [ ] Send a test → check it on mobile: columns stack, button is tappable,
      {{name}}/{{email}} filled.
- [ ] The source of truth lives at docs/email-templates/maison-master.html —
      paste it into live admin when going to prod.
