# Tests #138–#143 — Round 7: campaign visibility, enforced free delivery, offers rework, support codes, HTML emails, admin selects

## 1. Campaigns actually show (#138)

- [ ] /admin/campaigns: your ACTIVE in-window campaign shows a pulsing **LIVE**
      badge and the exact text the bar displays. Non-live rows say why
      (paused / starts in future / ended).
- [ ] With banner text EMPTY, the storefront bar shows the **campaign name**
      ("One Year Festival…") — plus "use code …" when a code is set.
- [ ] Pausing/activating updates the storefront bar immediately (hard refresh).

## 2. Per-product free delivery is real now (#139)

- [ ] Flag "Free delivery" on product A only. Cart with ONLY product A →
      choose delivery at checkout → shipping shows **৳0** (FREESHIP-ITEMS chip
      appears in the promo list).
- [ ] Add any unflagged product B → the code drops off; shipping charges again.
- [ ] FREESHIP-ITEMS never appears on /offers.

## 3. Codes are private by default (#140)

- [ ] Cart/checkout: **no suggested codes** anywhere — just the input.
- [ ] /admin/discounts: each row has a **Public/Hidden** pill. Everything starts
      Hidden.
- [ ] Toggle WELCOME10 → Public → **/offers** (footer "Offers & Codes") lists it
      with tap-to-copy; free-delivery-over-৳X banner shows when configured.
- [ ] Toggle back to Hidden → /offers drops it (within ~5 min or after a
      campaign change).
- [ ] Signed-in: /account shows **My offers** — public codes + (if the phone
      reward is on and the account has a phone) the personal PH- code.
- [ ] **Code batch**: prefix TEST, 25 codes, 10% → CSV downloads with 25 rows;
      the discounts table does NOT list them (a note says N machine codes are
      hidden); one code from the CSV works at checkout exactly once.

## 4. Support-call discount (#141)

- [ ] /admin/abandoned-carts: set the incentive % → click **Code** on a row →
      toast shows AB-XXXXXX + % and it's on the clipboard.
- [ ] That code works at checkout once; clicking Code again returns the SAME code.

## 5. HTML email templates (#142)

- [ ] Any template body has **Visual | HTML source** tabs; source is a
      monospace box.
- [ ] Paste a full `<!DOCTYPE html>` document into a custom template → the
      preview shows it WITHOUT the Maison shell; `{{name}}`-style placeholders
      fill in the preview and in a test send.
- [ ] A normal (fragment) body still previews inside the branded shell.

## 6. Admin selects (#143)

- [ ] Dropdowns across /admin (discounts, campaigns, shipping, etc.) are the
      shadcn-style combobox — no native browser select.
- [ ] Optional selects (e.g. Category/Collection pickers) show an **X** when a
      value is chosen — clicking it clears without a page refresh.
